# CHANGELOG.md — RingOut

Alle abgeschlossenen Änderungen am Projekt, neueste zuerst.

---

## [Unreleased]

### Features
- feat(arena-football): **Gold-Kanten- und Einrastfeedback fuer die Elimination-Umbauten** — beide Arenawechsel (4 → 3 und 3 → 2) bekommen ein rein visuelles Feedback (2026-08-27). Waehrend des Umbaus wandert eine weiche Aktivierung von innen nach aussen durch die drei Stufen der vorhandenen Goldstruktur (Spielflaechenkante → Bandenlinie und Bande → Aussenkante und Gesims); am exakten Arena-Lock folgt ein kurzer Impuls von ~0.15 s mit quadratischem Abfall. Das **ausgeschiedene Tor** verliert seine Teamenergie jetzt weich waehrend der Torneuordnung statt am Ende hart umzuschalten — die verbleibenden Tore bleiben unberuehrt. Traeger ist ausschliesslich die Goldstruktur: Deck und Bande haengen an einem System, das transparente Bandenglas leuchtet nie selbst. **Kein neues FX-System**, keine Partikel, kein Screen-Flash, kein Audio, keine Kameraaenderung. Die Intensitaet wird in jedem Frame aus dem bestehenden Morph-Fortschritt abgeleitet — kein eigener Zustand, kein Timer, ausserhalb einer Transition exakt 0. Nachgewiesen ohne Gameplay-Einfluss (identischer Koerper-, Phasen-, Wertungs- und Ablaufverlauf) und mit bitgleichem 4 → 3 Morph (19075 Ringpunkte, Abweichung 0.0e+0). Classic und Tactical unveraendert.
- fix(arena-football): die Gold- und Markierungsmaterialien der Arena werden nur noch **einmal** angelegt statt bei jedem Aufbau neu geklont — waehrend einer Transition wird die Arena dutzendfach neu gebaut, wodurch bisher pro Umbauschritt ein weiteres Materialobjekt entstand (2026-08-27).
- feat(arena-football): **Arenawechsel 3 → 2 als Morph** — auch der zweite Umbau wird jetzt animiert statt hart getauscht; die finale Shouldered-Wide-Arena wird sichtbar aus der Drei-Tore-Arena aufgebaut (2026-08-27). Derselbe Pfad wie 4 → 3 (Stuetzfunktions-/Minkowski-Interpolation, getrennte Torneuordnung vor dem Geometrieumbau, Kamera fest, Koerper eingefroren, Ball erst nach dem Einrasten): erst fahren die beiden Ueberlebendentore auf dem kuerzesten Winkelweg auf ihre 180-Grad-Achsen und das ausgeschiedene Tor zieht sich zurueck, dann formt sich die Bande um. Der schwierige Teil sind die **unterschiedlichen Formfamilien**: sechs Kernecken mit Eckradius 3.50 gegen acht mit 2.60. Beides laeuft ohne Sonderlogik — die Minkowski-Kombination fuehrt alle Kantenrichtungen mit (die vier Schulterflaechen wachsen dadurch stetig aus Laenge 0, die dritte Torseite zieht sich stetig auf 0 zurueck), und der Eckradius wird im selben Fortschritt linear mitgefuehrt. Gemessen ueber 201 Stufen x 720 Richtungen: 0 Gegenbewegungen, 0 Ueberschwingen, beide Endzustaende exakt. **Keine zweite Morph-Engine**, eine gemeinsame Zeittabelle (Hold 12 / Tore 24 / Arena 54 / Settle 10 Ticks ≈ 1.67 s, danach 0.5 s Balldrop). Der 4 → 3 Morph ist dabei nachweislich **bitgleich** geblieben (19 075 Ringpunkte, Abweichung 0.0e+0). Keine Physik-, Kamera-, FX- oder Audioaenderung; Classic und Tactical unberuehrt.
- feat(arena-football): **finale adaptive Elimination-Arenaformen** — die drei Phasen haben jetzt jeweils eine eigene, manuell freigegebene Gameplay-Identitaet (2026-08-26). **4 Spieler:** Rounded Square `17.50`, Eckradius `3.50`, Spawn `11.50` — **unveraendert**. **3 Spieler:** **Broad Rounded Triangle** — gleichseitiges Dreieck mit gekappten Spitzen, Apothem `12.50`, Eckradius `3.50`, Spawn `8.15`; drei lange Torseiten fuer Bank-Shots, drei kurze Kappflaechen statt spitzer Ecken, exakte 120-Grad-Symmetrie. Loest die Ecken auf, in denen der Ball frueher aussen herumlief, **ohne** den Eckradius zu erhoehen. **2 Spieler:** **Shouldered Wide** — breites Achteck `15.60 × 11.60`, Eckradius `2.60`, Spawn `10.15`; zwei lange Hauptbanden, zwei flache Torwaende und vier diagonale Schulterflaechen davor. Die Schultern geben einem aussen laufenden Ball einen neuen Anschlusswinkel — spuerbar mehr Raum, weniger geschenkte Abschlusspositionen nach einem Aussenlauf, mehr Single- und Double-Bank-Wege. Neu im Code: gemeinsamer Halbebenenschnitt `fbHalfPlanePoly` mit den Generatoren `fbTruncTri` und `fbShoulderRect`, Bandengrenze fuer konvexe Kernpolygone (`footballPolySD`) und der passende Renderer-Pfad. **Keine Physikaenderung** (M1, Reibung, Restitution, Ballradius 25, Spielerradius 32, fester Zeitschritt, CCD, lichte Torbreite 227.84 unveraendert), keine Aenderung an Classic oder Tactical, kein Online.
- feat(arena-football): **Arenawechsel 4 → 3 als Morph (Transition V3)** — der erste Umbau wird animiert statt hart getauscht (2026-08-26). Start- und Endform werden ueber ihre **Stuetzfunktion** interpoliert (Minkowski-Summe der Kernpolygone), wodurch sich die Wand in **jeder** Richtung monoton von ihrem Start- auf ihren Endwert bewegt: keine einknickende Zwischenform, keine groesser als die Ausgangsarena, kein Ueberschwingen. Dramaturgie in vier Abschnitten (Hold 12 / Tore 24 / Arena 54 / Settle 10 Ticks ≈ 1.67 s) — die Tore werden zuerst getrennt neu gesetzt, danach faehrt die Bande um. Kamera fest auf dem Phase-4-Framing, Eingaben gesperrt, alle Koerper eingefroren, Balleinwurf erst nach dem Umbau; die Tore sitzen ueber eine Sehnenkonstruktion auch waehrend des Morphs vollstaendig auf dem Deck. Der Wechsel **3 → 2 bleibt bewusst ohne Animation** und ist weiterhin der bestehende harte Rebuild.
- feat(arena-football): **Elimination in die normale Modusauswahl integriert** — Arena Football hat jetzt **drei sichtbare Modi** (2026-08-26). Das bestehende Modal `#fbModeOv` zeigt in fester Reihenfolge `CLASSIC 1V1`, `TACTICAL 1V1` und `ELIMINATION`; **Classic bleibt Standard und die einzige hervorgehobene Option** (`.vopt.rec`), Elimination wird bewusst nicht empfohlen. Jede Option startet direkt ueber den bestehenden einzigen Startpfad `startFootball(variant)` — kein zweiter Startpfad, kein Zwischenbildschirm, keine zweite Bestaetigung, dieselbe Haptik und derselbe Zurueck-Flow wie bisher. Der Dev-Direktlink `?dev=1&fb=elimination4` ueberspringt weiterhin nur das Modal und fuehrt nachweislich in exakt denselben Startzustand wie der Menuebutton. Neue i18n-Strings in EN/DE/TR (`4 PLAYERS · CONCEDE ONCE = OUT` / `4 SPIELER · 1 GEGENTOR = RAUS` / `4 OYUNCU · 1 GOL YE = ELEN`). **Keine Gameplay-, Arena-, Physik-, HUD- oder Kameraaenderung** in Classic, Tactical oder Elimination; keine Arena-Transitionsanimation; Elimination bleibt lokal/Hotseat, Online ist unveraendert nicht implementiert und ueber die Modusauswahl nicht erreichbar.
- feat(arena-football): **Elimination** als dritte Football-Variante ergaenzt — stabiler lokaler Dev-Modus, erreichbar ausschliesslich ueber `?dev=1&fb=elimination4` (2026-08-26). **Classic 1v1 und Tactical 1v1 sind unveraendert** (Arena, Spawns, HUD, Score, Commit/Reveal, Tor, Goal-FX, Goal-Sound, Kamera, Menue, Tests). **Regeln:** 4 Spieler mit je einer Figur plus einem neutralen Ball; verdecktes Commit fuer alle aktiven Spieler, danach ein gemeinsamer Reveal mit simultanem Start; **ein Gegentor = sofort ausgeschieden** — kein Timer, keine Gegentor-Punkte, kein Tiebreak; das Tor eines ausgeschiedenen Spielers wird physikalisch zur Bande und optisch als totes Tor gezeichnet; der letzte verbliebene Spieler gewinnt, nach dem entscheidenden Tor faellt kein neuer Ball mehr. **Adaptive Arena 4→3→2→1:** die Spielflaeche folgt der Spielerzahl — Rounded Square (`17.50` BR, 4 Tore im 90-Grad-Raster) → abgerundetes Dreieck (Apothem `11.50` BR, 3 Tore im 120-Grad-Raster) → Rounded Rectangle (`14.00 × 10.50` BR, 2 gegenueberliegende Tore). Eine einzige Torgeometrie bedient alle drei Phasen ueber `footballFold()`, das einen Punkt in das kanonische +X-Bezugssystem des naechstliegenden Tores dreht; ausserhalb von Elimination ist die Faltung die Identitaet, der Bestandspfad rechnet bitgenau wie zuvor. Die Eckrundung wurde von `9.00` auf `3.50` BR reduziert (gekruemmter Konturanteil 45.4 % → 16.4 %), damit der Ball weniger dauerhaft aussen zirkuliert — rein geometrisch, ohne Zentrumskraft und ohne Physik-Retunes. **Fairer Respawn:** nach jeder Eliminierung werden alle Ueberlebenden auf die symmetrischen Spawns der neuen Phase gesetzt (Velocities 0, Ball faellt zentral von oben, neuer Hidden-Commit-Zyklus) — keine Positionsvorteile und keine Spawn-Traps aus der vorherigen Phase. Arenawechsel und Aufstellung passieren im selben Anweisungsblock; `placeBalls()` und der Respawn teilen sich mit `fbElimSpawnX/Y(slot)` **eine** geometrische Spawnregel. Torzuordnung deterministisch: aktive Spieler-IDs aufsteigend auf die Slots `0..n-1`. **Unveraendert:** Torbreite `227.84`, M1-Preset, Ballradius `25`, Spielerradius `32`, Goal-FX, Goal-Sound, Kamera-Framing-Logik. **Online ist nicht implementiert** — Elimination ist rein lokal/Hotseat. Neue Regressionssuite `tools/test_football_elimination4.js` (843 Assertions) im Runner als `Football-Elim4`.
- fix(arena-football): Bandengrenze der Dreiecksarena (3-Spieler-Phase) korrigiert (2026-08-26) — `footballTriSD()` behandelte den uebergebenen Apothem als bereits geschrumpften Wert, waehrend Aufrufer und Renderer den Abstand zur **fertigen** Seite uebergeben. Die Kollisionsgrenze lag dadurch um `Eckradius − 2×Koerperradius` zu weit aussen: Ball und Spielerfiguren liefen sichtbar durch die gezeichnete Bande (gemessen +87.0 bzw. +80.0 Einheiten auf allen drei Seiten). Der Fix rechnet Innenform und Umkreisradius aus `ap − rc`; die Phasen 4 und 2 waren nie betroffen und bleiben bitgenau. Regression: die Suite misst die Bandendistanz je Phase und schlaegt bei Wiedereinbau des Fehlers mit 12 gezielten Fehlern an.
- feat(arena-football): **Tactical 1v1** als zweiter Arena-Football-Modus ergaenzt; **Classic 1v1 bleibt der Standard** (2026-08-25). Arena Football hat jetzt genau zwei Produktmodi, waehlbar ueber ein neues Modal `#fbModeOv` mit derselben Struktur wie das bestehende Bot-Format-Modal (`CLASSIC 1V1 - 1 Figur pro Spieler` als hervorgehobener Standard, `TACTICAL 1V1 - 2 Figuren / 1 Zug pro Runde`; i18n en/de/tr). **Classic ist unveraendert:** 1 Figur je Spieler, 3 Bodies, bestehende Spawns, verdecktes Commit/Reveal, simultaner Start beider Figuren, First to 3, Torlogik, Goal-FX, Goal-Sound, HUD, Reset, Kamera. **Tactical:** 2 Figuren je Spieler (B1/B2, R1/R2) plus neutraler Ball; pro Runde waehlt jedes Team verdeckt GENAU EINE eigene Figur und committet dafuer einen Zug — beim Reveal starten genau die gewaehlte blaue und die gewaehlte rote Figur ihre Impulse vor dem ersten gemeinsamen Physikschritt, die beiden nicht gewaehlten Figuren erhalten keinen Startimpuls, bleiben aber vollstaendig kollidierbar (blocken, lenken den Ball ab, koennen getroffen werden). Nach normalem Settlement bleiben alle Positionen erhalten; nur nach einem Tor werden alle fuenf Bodies auf die Tactical-Spawns zurueckgesetzt. Aufstellung `FOOTBALL_TACTICAL_SPAWN`: offensive Figur (+-6.40, -2.80) BR, tiefe Figur (+-12.20, +4.60) BR, Rot an der Mittelachse gespiegelt, Ball exakt im Mittelpunkt; die tiefe Figur steht ausserhalb der lichten Torbreite. Auswahl-UX: dezenter Bodenring in Teamfarbe (waehlbar 0.34 / gewaehlt 0.95 und 1.10x groesser), keine dauerhaften Figurenlabels; Hidden Information bleibt gewahrt — waehrend der Aim-Phase traegt nur das Team am Zug einen Ring, im Reveal die beiden gestarteten Figuren. **Architektur:** keine zweite Zug-Zustandsmaschine — `fbVariant` ist die einzige Weiche, `startFootball(variant)` der einzige Startpfad (clamped jeden fremden Wert auf Classic), Tactical fuegt der bestehenden Pipeline nur die Information hinzu, WELCHE eigene Figur zieht (`fbSel`, vor dem Commit; danach ist `commitIdx` wie in Classic die Wahrheit). Dev-Direktlinks `?dev=1&fb=classic` / `?dev=1&fb=tactical` ueberspringen die Auswahl, jeder andere Wert faellt auf Classic zurueck. **Online unveraendert:** Arena Football ist in beiden Modi rein lokal — `mode='football'` wird nur im Startpfad (`online=false`) und in der Menue-Vorschau gesetzt, Online-Raeume tragen kein `mode`-Feld; es gibt keine Protokollaenderung. **Physik, Arena B, Movement M1, Ballradius 25, Torgeometrie, Score, Goal-FX und Goal-Sound sind unveraendert.** Neue Produktsuite `tools/test_football_tactical.js` (219 Assertions: Modus-Separation inkl. Fallback und Moduswechsel, Aufstellung, Auswahl, Commit/Reveal, Simultanitaet, Kollisionen, Privacy, Settlement, Tor/Reset/First-to-3, Determinismus) im Runner registriert; Football-Shell 818/0, Football-Flow 148/0, Football-Arena 61/0, Golden-Physik 13/0, Ring-Collapse 235/0, dieselben fuenf bekannten Legacy-Suiten bleiben rot, keine neue Suite rot, `git diff --check` sauber (2026-08-25)
- chore(arena-football): Prototypvariante "Tactical Dual" (beide Figuren je Team gleichzeitig planbar) nach Spieltest verworfen und restlos aus dem Produktivcode entfernt (2026-08-25). Befund im manuellen Test: unuebersichtlicher, erheblich defensiver, deutlich weniger Tore, schlecht lesbarer Spielverlauf. Entfernt wurden Variante, Planungsstate, Team-Commit-Button, No-Move-Planung, Vierfach-Launch-Hook und alle zugehoerigen Ring-/Guard-Zustaende; die Prototyp-Suite ist aus `tools/run_all_tests.js` genommen und liegt untracked unter `artifacts/football-tactical-dual-prototype/`. Eine maschinelle Assertion prueft, dass kein Dual-Bezeichner mehr im Produktivcode steht (2026-08-25)
- feat(arena-football): Rounded-Rectangle-Arena finalisiert (2026-08-07). Die Football-Spielflaeche ist jetzt produktiv ein **Rounded Rectangle** (Arena B der Prototypphase): Innenlaenge **1152**, Innenbreite **812.8** (1.417:1), Eckradius **219.2**; zentrale Konfiguration `FOOTBALL_ARENA`, physikalische Grenze als Signed-Distance-Form (`footballShapeSD`/`footballBoundSD`, C1-stetige Eckuebergaenge, exakte Kreisboegen). **Tor buendig integriert:** gerades Torasset `assets/arena_football_goal.glb` (jetzt getrackt), Sockelvorderkante exakt auf der Bandeninnenflaeche (`postFront == halfLen`) → keine Ballfang-Tasche neben dem Tor; lichte Torbreite **227.84**, Torlinie bei 651.776. **Movement M1 final:** Spieler-Daempfung 0.9976, Ball-Daempfung 0.9982 (getrennt), Auslauf slowv 0.50 / fend 0.9905, stopv 0.050, restBall 0.44, Bande 0.60, Pfosten 0.50. **Neutraler Ballradius 25** (Spieler bleiben 32); `ballRad(b)` ist die einzige Radiusquelle fuer Physik und Rendering; Tor/Durchmesser ≈ 4.56. Sichtbare Arena (Spielflaeche, Randweg, Sockel, Bande, Markierungen, Teamfarb-Toenung hinter den Toren) entsteht prozedural aus denselben Parametern; Band-GLB dient nur noch als Materialquelle, das gekruemmte Tor der Kreisarena wird nicht mehr geladen. Alle Prototyp-Umschaltungen (Arena 1–4, Movement/Ballgroessen 5–8) entfernt — der Modus startet direkt mit dem freigegebenen Stand. Manuell freigegeben nach Spieltests der Prototypphasen (Arena A/B/C, M1–M3, B0–B3; lokale Artefakte unter `artifacts/`). RingOut-Modi, Netzwerk, Audio, HUD, Score-/Goal-Logik unveraendert (Golden-Physik 13/13, Ring-Collapse 235/235). (2026-08-07)
- feat(arena-football): UX-Phase 3 — Premium-Torfeedback und Celebration Flow (Commit `babbbe78ee388489321d1f0cb3e032bbaabd0725`). **(1) Premium Goal-Feedback:** Beim bestätigten Tor wird der **getroffene** Torbereich kurz hochwertig aktiviert — beteiligt sind genau die drei bereits farbig gefassten Bauteile aus UX-Phase 1: **Emblem**, **vordere Gold-Keyline** und **Torlinie** (letztere trägt zugleich den Öffnungs-/Walkway-Akzent). Feedbackfarbe ist **warmes Gold** (`GOAL_FX_GOLD = 0xffd79a`), das mit maximal 72 % Anteil (`GOAL_FX_MIX`) über die Ruhefarbe gelegt wird; die **bestehende Teamfarbe bleibt die Zuordnung** und damit lesbar — Gold sagt „Treffer", die Teamfarbe sagt „welches Tor". Der Zuwachs der Emissive-Intensität ist ein Faktor auf den jeweiligen Ruhewert (Emblem ×2.00, Keyline ×3.40, Torlinie ×0.90 auf der Spitze), so dass Pegel 0 exakt den Ruhezustand herstellt. Bewusst **kein Confetti, keine Partikel, kein Textbanner, kein Sound, keine Kameraanimation, kein Kamera-Shake**. **(2) HUD-Feedback:** Es reagiert **ausschließlich die punktende Score-Seite** — die andere bleibt vollständig unangetastet. Score-Pop `FB_POP_MS = 460 ms` (Gold-Impuls, der in die Teamfarbe zurückfällt, Spitzenskalierung 1.21), Panel-Schimmer und Kanten-Glow der punktenden Seite `FB_GOAL_HUD_MS = 1200 ms`. **Keine zweite Score-Engine** — `score[]` aus der Spiellogik bleibt Source of Truth, das HUD vergleicht nur den zuletzt angezeigten Wert; es gibt genau zwei einmalige `setTimeout`, kein `setInterval`. Alle CSS-Regeln sind weiterhin `#game.fb`-gescoped. **(3) Nachglut:** Goal-FX-Gesamtdauer `FB_GOAL_FX_MS = 1500 ms` mit schnellem Anstieg von rund **112 ms** (`FB_GOAL_FX_ATTACK = 0.075`, Smoothstep) und anschließendem **quadratischem** Ausklang (`q²` statt `q³`) — der kubische Ausklang war nach einem Drittel der Nachglut praktisch unsichtbar, so trägt die Goldglut sichtbar weiter und läuft trotzdem mit Steigung 0 in den Ruhewert (keine Abschaltkante, kein Strobing). Diese spätere Timing-Korrektur hat **keine Intensität** verändert: Farbe, Mix und alle drei Gains sind unangetastet geblieben. Zeitbasis ist bewusst die Renderuhr (`performance.now`), nicht der Physik-Tick — der Impuls ist Präsentation und berührt den Fixed-Step nicht; der Renderer schreibt je Material nur bei Pegeländerung und im Ruhezustand gar nicht. **(4) Celebration Flow:** Die **bestehende** Goal-State-Maschine wurde um eine Wartephase erweitert: `play → fall → celebrate → spawn → play`, beim Matchpunkt `play → fall → celebrate → result`. Normales Celebration Window `FOOTBALL_GOAL_CELEBRATE_TICKS = 51` (≈ 850 ms bei 60 fps), Matchpunkt `FOOTBALL_GOAL_WIN_CELEBRATE_TICKS = 66` (≈ 1100 ms); die Länge wählt `footballCelebrateTicks()` am kanonischen `footballWinner` — kein zweites Flag. Kern der Umsetzung: `footballResetRound()` wanderte vom Fallende an das **Ende** der Wartephase, dadurch **existiert der neue Ball während der Feier gar nicht** und erscheint erst danach (≈ 2050 ms nach dem Tor, also erst nach dem vollständigen Ablauf des 1500-ms-Impulses); die nächste Runde ist nach ≈ **2550 ms** wieder spielbar. **Keine setTimeout-basierte Gameplaylogik** und **keine zweite State-Maschine** — die Phase tickt im selben Fixed-Step wie alles andere. Spieler und Eingaben bleiben über den **vorhandenen** Goal-State neutralisiert (`footballGoalBusy()` greift für jeden Zustand ≠ `play`, `footballFreezePlayers()` läuft unverändert) — keine neue Sperre, keine globale Pausefunktion. Beim Matchpunkt erscheint das Result-Overlay zu keinem Zeitpunkt während der Celebration; der Trefferimpuls läuft vollständig ab, bevor die bestehende Result-Struktur übernimmt. Aufgeräumt wird über `footballResetMatchState()` (neues Match und Menüwechsel), `footballMatchEnd()` und `fbClearHudFx()`. **Abgrenzung:** keine Änderung an Physik, `FOOTBALL_PHYS`/GLIDE, Anti-Wedge, Goal Detection, Scorelogik, First-to-3-Regel, Kamera, GLBs, Netzwerk, Spawnposition oder Spawnbewegung. Belegt nicht nur per Diff, sondern per byteweisem Vergleich von 24 geschützten Codeblöcken gegen den Vorgänger-HEAD — alle identisch; einzige Ausnahme `footballTryGoal` mit **null entfernten und genau einer hinzugefügten Zeile** (dem FX-Trigger). Verifizierte Teststände: **Football-Shell 738/0** (589 → 660 → 677 → 738), **Football-Flow 118/0**, **Golden-Physik 13/0**, **Ring-Collapse 235/0**; dieselben fünf bekannten Legacy-Suiten (FFA-Kern, FFA-Online-Flow, FFA-Online-Race, Reconnect-B2, Team-Duel) bleiben rot, **keine neue Suite rot**, `git diff --check` sauber (2026-08-06)
- feat(arena-football): UX-Phasen 1 und 2 — Torzuordnung und Match-HUD (Commits `e162c51`, `c4c9135`). **(1) UX-Phase 1 — Torzuordnung und Kugellabels** (`e162c51`, „clarify goals and remove ball labels"): Im Football-Modus tragen die Kugeln kein Namenslabel mehr. Beide Stellen in `drawBall()` sind nur mode-gescoped übersprungen (`&&mode!=='football'`), die Labelstruktur selbst bleibt erhalten — alle anderen Modi zeigen ihre Namen unverändert. Damit verschwindet auch die falsche Beschriftung des neutralen Balls: `pName(owner 4)` hatte ihn als „Spieler 5" ausgewiesen, obwohl er keinem Spieler gehört. Die Torzuordnung ist jetzt farblich eindeutig: jedes Tor trägt die Farbe der Mannschaft, die es **verteidigt** — Blau verteidigt das −X-Tor (Blau startet dort, `placeBalls`: `cx-off`), Rot das +X-Tor; das ist spiegelbildlich zur Wertung (`footballGoalSide`: Durchtritt bei +X = Punkt für Blau). Drei abgestimmte Akzente, alle aus bereits im Asset vorhandenen Bauteilen und über den Objektnamen adressiert, weil alle Goldteile dasselbe Material teilen: farbiges Band bündig im Randweg mit Innenkante auf der Walkway-Innenlippe (Breite 0.30 GLB, Bogen 29.27° = exakt die lichte Toröffnung aus `footballGoalClearHalf()`, opak mit Polygon-Offset → kein Durchscheinen, kein Z-Fighting, kein Überstand ins Spielfeld), Emblem-Inset `AF_Goal_Emblem` mit eigenem Farbmaterial, und ein dezenter Teamfarben-Schimmer auf der vorderen Gold-Doppel-Keyline (`AF_Goal_GoldInner/-Outer`), der die Zuordnung in die Senkrechte trägt, wo ein Bodenband bei flacher Kamera verkürzt. Farben sind `PCOLS[…].m3`, also dieselben Werte wie die Spielerkugeln. Marmor, Glas, Hauptrahmen, rückseitige Keyline und die Torform sind unverändert; **keine GLB-Änderung**. **(2) UX-Phase 2 — Match-HUD** (`c4c9135`, „add premium football score hud"): kompaktes Score-HUD in der Form `BLAU | 0 : 0 | ROT` mit dem Untertitel `ERSTER BIS 3`. Es gibt **keine zweite Score-Engine** — das HUD schärft die bestehende Statusleiste (`#card0` | `.scorebox` | `#card1`) nach, sobald `#game` die Klasse `.fb` trägt, und liest ausschließlich `score[]` aus der Spiellogik. Score 30 px gegen 11 px Teamname, `tabular-nums` mit fester Ziffernbreite → kein Layoutsprung bei 0→1→2→3; dunkles Glas mit `backdrop-filter`, Goldkante am Score-Panel, Teamakzent nur als 2-px-Kante (blau links, rot rechts), eigener Kompaktzustand unter 380 px Breite. Die Zahl im Untertitel stammt ausschließlich aus `FOOTBALL_WIN_SCORE`; die i18n-Einträge `fbFirstTo` (de/en/tr) enthalten nur den Platzhalter `{n}`, keine Ziffer — es gibt also keine zweite Matchregel. Angezeigt wird die deutsche Fassung, weil die gesamte In-Game-Ebene deutsch ist („Runde N", „BLAU"/„ROT") und `LANG` ohne gespeicherte Auswahl auf `'en'` fällt; die globale Sprachlogik blieb unangetastet. Beim Tor skaliert **nur die geänderte Ziffer** 460 ms lang um 1.26 mit Teamfarben-Schimmer — rein visuell, ein einziger einmaliger Timer, kein `setInterval`, keine Partikel, kein Shake, keine neuen Sounds, keine Rückwirkung auf die Wertung. Entfernt wurden dabei: die sekundären Statuswörter „zielt…"/„bereit" (nur `.fb`-gescoped ausgeblendet, andere Modi behalten sie), ein zunächst gebauter Matchstart-Hinweis „BLAU → ROT · ROT → BLAU" (nach dem Browsertest **bewusst wieder vollständig zurückgebaut** — DOM, CSS, Timer, `newGame()`-Hook und Tests; kein Ersatz, keine Pfeile, keine Tutorialbox) und die alte Statuszeile der Integrations-Shell („BLAU zieht — Arena Football · Visuelle Integration", „Beide aufgedeckt…", „⚽ Arena Football — Shell"). Die echte Fehlermeldung „3D-Szene nicht verfügbar" bleibt bewusst erhalten — sie ist der einzige Hinweis auf einen nicht spielbaren Zustand. Das Result-Overlay (Gewinneranzeige, Endstand, „Neues Match", „Menü") ist unverändert. **Beide Phasen ohne jede Änderung an Arena, Toren als Geometrie, Kugeln, Kamera, Beleuchtung, Physik, GLIDE-Werten, Wedge-Logik, Goal Detection, Scorelogik, First-to-3-Regel, Matchende, Spawn, Reset, Netzwerk oder GLBs.** Eine maschinelle Assertion prüft, dass **jede** Regel des HUD-CSS-Blocks mit `#game.fb` beginnt (Ausnahmen nur Keyframe und Media-Query), weitere prüfen, dass `footballTryGoal`, `footballGoalSide`, `footballResetRound`, `footballTickGoal`, `footballMatchEnd` und `footballResetMatchState` keinen HUD-Code enthalten. Verifizierte Teststände nach beiden Phasen: **Football-Shell 589/0** (523 → 573 → 589), **Football-Flow 118/0**, **Golden-Physik 13/0**, **Ring-Collapse 235/0**; dieselben fünf bekannten Legacy-Suiten (FFA-Kern, FFA-Online-Flow, FFA-Online-Race, Reconnect-B2, Team-Duel) bleiben rot, keine neue Suite rot, `git diff --check` sauber (2026-08-06)
- feat(arena-football): Physikphasen 4A–4B-3 — Audit, iterative Kontaktauflösung, Tuning und Anti-Wedge (Commit `24fa968`). (1) **Phase 4A — Audit:** `tools/test_football_flow.js` als Messharness (extrahiert die echten Physikfunktionen aus `index.html`, kein DOM, kein Netzwerk, kein Zufall); Baseline dokumentiert in `artifacts/football-physics-audit/README.md`. Befund: kein Massenmodell, `REST = 0.25` für Ball/Bande/Pfosten gemeinsam, 2.50 px bleibende Ball-Ball-Überlappung, Verlagerung 0.00 px nach vier Volltreffern gegen einen wandgestützten Ball, `FRICTION === FEND` (Zwei-Regime-Entwurf wirkungslos). (2) **Phase 4B-1 — iterative Kontaktauflösung:** `FOOTBALL_CONTACT_ITERATIONS = 3`, nur `mode === 'football'`; Integration, Dämpfung und Spin laufen weiterhin genau einmal pro Micro-Step, wiederholt wird ausschließlich die Kontaktauflösung; Treffer-Feedback nur im ersten Durchgang (`if(it===0)`). Bleibende Überlappung 2.5009 → 0.0929 px, D4-Bandenkeil 127.9 → 276.3 px, keine Energiezunahme. Die Zielmarke `< 0.05 px` wird mit `ci = 3` bewusst nicht erreicht (erst ab `ci = 4`) — 0.0929 px sind 0.15 % von 2·BR und optisch unsichtbar; der Harness weist die Lücke als WARN aus. (3) **Phase 4B-2 — Presetvergleich:** CURRENT/GLIDE/ICE mit getrennter Restitution nach Kontaktart, Football-eigener Gleit-/Endphasendämpfung und eigener Stoppschwelle, dazu deterministische Wedge-Erkennung mit energieneutralem Escape; alle drei über 43 Szenarien vermessen. (4) **Phase 4B-3 — Finalisierung:** nach manuellem Browservergleich wurde **GLIDE freigegeben**, CURRENT (zu stumpf) und ICE (zu rutschig, zu lange Züge) verworfen. Produktiv gibt es seither genau einen Wertesatz `FOOTBALL_PHYS = { friction 0.9968, fend 0.9935, stopv 0.035, restBall 0.40, restBand 0.52, restPost 0.47 }` mit einem einzigen Zugriffspunkt `footballPhys()` — **keine Preset-Auswahl, kein Debug-Schalter, kein URL-Parameter**; CURRENT und ICE existieren nur noch als Vergleichsmodelle in `tools/test_football_flow.js`. Wirkung: Ausrollzeit des Neutralballs 3.53 → 6.83 s, Ausrollstrecke 362.6 → 768.9 px, Banden-Rückprall 0.25 → 0.52, Pfosten-Rückprall 0.25 → 0.47, maximale Pinning-Dauer 15 → 10 Frames. Der reproduzierte Browserbefund (drei Kugeln zwischen Bande und Torsockel, drei gezielte Volltreffer) löst sich statt 0.10 px Verlagerung jetzt nach 411 Frames (6.85 s) und 275 px. `LAUNCH`, `MAXPULL_FRAC`, Spin-Konstanten und die globalen `FRICTION`/`FEND`/`STOPV`/`REST` sind unverändert; **kein Massenmodell**, keine Maximalgeschwindigkeit, keine Visual-/Netzwerkänderung; alle Nicht-Football-Modi laufen bit-identisch weiter (Golden-Physik 13/13, Ring-Collapse 235/235, Lockstep 24/24). Tests: Football-Shell 416 → 523, neue Football-Flow-Suite 118 (43 Szenarien × 3 Vergleichsmodelle, Konvergenz-Sweep, Determinismus-Fingerprints), beide im Runner registriert; zwei aufeinanderfolgende Flow-Läufe byteidentisch, `git diff --check` sauber (2026-08-06)

### Fixes
- fix(arena-football): Review-Fixes der Arena-Finalisierung (2026-08-08). **(1) Torkanal-Totzone (P1):** Auf dem Rechteck konnte ein langsam durchgetretener Ball VOR der Torlinie liegenbleiben, jenseits der maximalen Spielerreichweite (halfLen + Ballradius = 601) — das Match war dauerhaft blockiert (auf der Kreisarena geometrisch unmoeglich; reproduziert mit Einschussgeschwindigkeit 0.8: Endlage |dx| ≈ 649). `footballGoalSide` wertet jetzt einen RUHENDEN (v < stopv), unerreichbaren Kanalball als Tor der Seite, in deren Kanal er liegt — deterministisch, tickbasiert, das regulaere Linienkriterium bleibt unveraendert; Regressionstest in `test_football_arena.js` (61/0). **(2) Render-Fallback:** Faellt das Band-GLB (Materialquelle der prozeduralen Arena) aus, bleibt die runde Plattform als Notboden sichtbar (`rectReady`), statt Baelle und Tore vor dem Himmel schweben zu lassen. **(3) Kreisformel-Reste im Renderer:** Randstufen-Lift der Kugeln folgt im Football-Modus jetzt der Rounded-Rectangle-Kante (vorher hoben Kugeln vor den Toren ab bzw. clippten an den Laengsbanden in die Stufe); der Ring-Out-Warnglow (2D-Overlay) ist im Football aus (dort gibt es keine Elimination). **(4) Kamera:** horizontaler Framing-Bedarf deckt die Deck-/Toraussenkante (postBack + Ballradius) — schmale Viewports beschneiden die Tore nicht mehr. **(5) Deck-Kante:** Aussenkante der Plattform wird aus Torlinie + neutralem Ballradius (25) berechnet statt mit BR=32 hergeleitet — der Ball faellt exakt an der sichtbaren Kante. Dazu: veralteter Torlinien-Kommentar (radiale Kreisherleitung) korrigiert, toter Guard in `curSLOWV()` entfernt, Prototyp-Kamerahook `window.__fbDev`/`devCamLock` ausgebaut, Teststand-Tabelle in PROJECT.md synchronisiert (Shell 818/0, Flow 148/0, Arena 61/0). (2026-08-08)
- fix(arena-football): Anti-Wedge gegen tote Mehrfachkontakte (Teil von Physikphase 4B-2/4B-3, Commit `24fa968`) — im Browsertest konnten sich drei Kugeln zwischen Bande und Torsockel dauerhaft verkeilen und lösten sich auch durch weitere Volltreffer nicht. Ursache: die Iteration aus 4B-1 löst die Überlappung, nicht die Geschwindigkeitsstarre — in einem geschlossenen Keil zeigt jede zulässige Verschiebung in ein anderes Hindernis, und `STOPV = 0.10` erklärte den Zug zwei Frames später für beendet. Neu: eine benannte, rein geometrische Keildefinition (≥ 2 unabhängige Kontakt-Normalen, ein Normalenpaar mit Skalarprodukt ≤ −0.45, `|v| < 0.25` px/Micro-Step, Fortschritt < 0.40 px/Frame, äußerer Druck > 0.01 px/Micro-Step, über 8 aufeinanderfolgende Micro-Steps) und eine deterministische Reaktion: aus einer festen Kandidatenliste wird die Richtung mit dem größten minimalen Skalarprodukt gegen alle Normalen gewählt und der Anteil entlang der am stärksten verletzten Normalen in bis zu drei festen Durchgängen herausprojiziert. Die vorhandene Geschwindigkeit wird nur **umgelenkt** (Betrag exakt erhalten → keine Energie); nur bei praktischem Stillstand greift ein eng begrenzter, benannter Mindest-Escape von `FOOTBALL_ESCAPE_MIN_V = 0.12` px/Micro-Step (14.4 px/s, 1.8 % der Launch-Obergrenze). Kein Zufall, keine Zeitabhängigkeit, keine Toleranzschleife — lockstep-tauglich deterministisch; nach jedem Escape läuft die Mindestdauer neu. Ein einzelnes frontales Kontaktpaar löst über 400 Frames nie einen Escape aus. Keine Sockel- oder Bandenpenetration, keine Kugel über der Launch-Obergrenze, Tor-/Score-/Reset-/First-to-3-Logik unverändert. Verbleibende Grenze: exakt frontales 1D-Pinning ohne geometrische Fluchtrichtung bleibt bestehen (physikalisch korrekt, nur über ein Massenmodell lösbar) (2026-08-06)
- fix: Paket A — letzte Blocker-Korrektur (**weiterhin lokal — kein Cutover/Deploy/Push**) — ersetzt die fragilen Nicht-FFA-Heuristiken der vorigen Korrekturrunde durch einen einheitlichen Room-State und einen atomaren Seat-Claim. (1) **Einheitlicher Room-State:** ALLE Modi (FFA, 1v1, 2v2) werden mit `state:'lobby'` erstellt; der State ist die alleinige Wahrheit für Rejoin-/Claim-/Recycling-Erlaubnis. Die frühere indirekte Nicht-FFA-Erkennung über fehlende Presence/`players`/`g` entfällt (`roomRejoinableState()` = `state==='lobby'` für alle Modi; `validateRoom` verlangt für 1v1/2v2 Lobby). FFA bleibt bis zum Host-Start in Lobby; **1v1/2v2 schaltet der erfolgreiche Gastclaim atomar** gemeinsam `p/1=true` + `players/1` + `state='playing'` — kein separater State-Write, kein Zwischenzustand. (2) **Atomarer Seat-Claim (`claimSeatSlot`):** ersetzt den sequenziellen Presence-dann-Roster-Ablauf durch EINEN Multi-Path-`update()` (`p/<seat>` + `players/<seat>` [+ `state`]); alle Pfade gelingen gemeinsam oder der ganze Write wird abgelehnt (kein Teilzustand). Die Write-once-Presence bleibt der alleinige Arbiter → zwei parallele Claims gewinnen nie denselben Seat. **Kein Vorab-Löschen fremder Records:** ein stale Fremd-Record wird ausschließlich INNERHALB desselben atomaren Claims recycelt (Rules: nur bei freier Pre-Presence + Lobby). onDisconnect wird VOR dem Write armiert, nach Erfolg aktiv gelassen, nach Ablehnung abgebrochen; Op-Token wird VOR **und NACH** dem Write geprüft — ein stale Vorgang rollt nur eigene Writes zurück (Rejoin behält den durablen Record) und fasst nie fremde Daten an; onDisconnect-/Cleanup-Fehler werden geloggt, ein echter Transportfehler wird sichtbar gemeldet (nicht als „Sitz belegt" verschluckt). (3) **Rules (`firebase.rules.json`):** State-Knoten und Claim-/Recycling-/Rejoin-Gates für ALLE Modi an `state` gebunden (Claim/Recycling/Host-Rejoin nur in Lobby; keine Record-Erstellung/-Ersetzung/Presence-Wiederherstellung bei `playing`); die players-Create/Recycle-Zweige prüfen die im selben Multi-Path-Write gesetzte Presence über `newData` (merged), Recycling eines Fremd-Records nur bei pre-freier Presence (kein Seat-Diebstahl); id bleibt unveränderlich; `state` `lobby→playing` nur mit `p/1` present. (4) **Namensfeld:** `maxlength` 16→48 UTF-16-Units (passend zum Rules-Bound); `sanitizeName` cappt weiterhin auf max. 16 sichtbare Grapheme. (5) **Tests:** Fake-RTDB beider Harnesses hat jetzt ein **atomares `update()`** (all-or-nothing, merged-Presence-Wissen) und spiegelt die einheitlichen State-Rules; neue Negativtests — RP1 (parallele Claims auf denselben Seat → genau ein Gewinner), RD1 (onDisconnect nach Erfolg aktiv / nach Ablehnung abgebrochen), RN1 (Recycle-Race ohne Vorab-Löschen), R3/R4 (atomarer Reject ohne Teilzustand), Nicht-FFA-State-Negative in Rules-/ValidateRoom-/Prep-Suiten (Gastclaim/Host-Rejoin bei `playing` abgelehnt, `state`-Transitionen). `rest_verify_v3.js` um 1v1/2v2-**Match-Claims per atomarem PATCH** erweitert (weiterhin nur `--live`). **Emulator-Beweis in `tools/e2e/spike.js`** gegen die echte `firebase.rules.json`: abgelehnter Multi-Path-Claim hinterlässt weder `p` noch `players`, zwei parallele Claims → genau ein Gewinner, Claim bei `state==='playing'` abgelehnt. Suiten: ValidateRoom 40→44, FFA-Online-Prep 40→41, Flow 107→116, Rules 100→106; **Runner 12/12 grün, `test:e2e:spike` (inkl. Atomclaim-Beweis) + `test:e2e:ffa` 3/3 gegen den echten JDK-21-RTDB-Emulator grün, `git diff --check` sauber.** Physik/Lockstep/Move-Daten byte-unverändert (Goldens 13/13). **Kein Commit, kein Push, kein Rules-Deploy** — Cutover weiterhin gebündelt nach Paket B (2026-07-14)
- fix: Korrekturrunde Paket A nach Codex-Review (BLOCKER/HIGH + zugehörige MEDIUM, **weiterhin lokal — kein Cutover/Deploy/Push**) — (1) **Paket-A-Grenze wiederhergestellt:** Rejoin funktioniert für FFA, 1v1 und 2v2 ausschließlich im echten Lobby-/Wartezustand. `attemptRejoin()` lehnt jeden laufenden Matchzustand ab (`roomRejoinableState()`: FFA nur `state==='lobby'`; 1v1/2v2 nur Host-Seat 0 solange kein Gast-Presence/-Record und keine Turn-Daten `g` existieren — ein Gast-1v1-Rejoin ist nie sicher, sein Join hat das Match hostseitig gestartet). Der Host-`p/0`-Rejoin ist auch in den Rules auf genau diesen Zustand begrenzt; `maybeStart()` kann einen laufenden Raum nach Rejoin nie lokal neu starten, Presence-Restore während `state==='playing'` ist client- UND rule-seitig unmöglich. (2) **Rules-Garantien für players/<seat>:** id ist unveränderlich, solange der Record existiert (kein Replace-Branch mehr — auch nicht bei gehaltener Presence); Record-Erstellung nur durch den aktuellen Presence-Halter (`p/$i===true`) und nie während `state==='playing'`; Record-Delete nur bei freier Presence (dadurch kann ein später Rollback einen inzwischen fremd übernommenen Record physisch nicht löschen); Seat-Recycling ausschließlich als Delete+Create in der Lobby bei freier Presence; Zusatzfelder weiter abgelehnt; `p/1` ist jetzt auch in 1v1/2v2 write-once (vorher bewusst offen). Name-Bound in den Rules auf 1–48 UTF-16-Units korrigiert (der alte 16er-Bound hätte legitime 16-Grapheme-Namen mit Emojis serverseitig abgelehnt → Claim-Abbruch). (3) **Claim-Lifecycle kompensiert (`claimSeatSlot`):** onDisconnect wird VOR dem Presence-Write armiert (Crash zwischen Presence-Gewinn und Roster-Write heilt sich selbst), dann Write-once-Presence als einziger Arbiter, dann Roster-Write; schlägt der Roster-Write fehl, wird die EIGENE Presence zurückgerollt und der Join bricht sichtbar ab (kein verschluckter Fehler, kein dauerhaftes `p/<seat>` ohne gültigen Record, kein verwaister Record); der Rollback fasst nie einen players-Record an (bei Rejoin-Refresh bleibt der alte gültige Record erhalten, nur Log). Stale-Record-Recycling löscht den fremden Record VOR dem Presence-Versuch (Rules erlauben das nur bei freier Presence — ein gerade übernommener Sitz ist geschützt). `createRoom`/`joinRoom` (1v1 und FFA) laufen über denselben Pfad. (4) **Op-Token-Guards:** jeder Create/Join/Rejoin-Vorgang erhält ein Op-Token (`joinOpSeq`), das nach jedem `await` geprüft wird; `leaveOnline()` oder ein neuerer Vorgang entwertet alte sofort (auch vor dem Early-Return); Globals (`roomCode/myPlayer/online/mode/gen/fmt/winTarget`) werden erst nach vollständig erfolgreichem Remote-Ablauf übernommen, ein stale gewordener Vorgang rollt nur die eigenen, noch unveränderten Teilwrites zurück (`releaseSeatSlot`/`releaseSeatClaim` mit id-Check) und hinterlässt weder Listener noch Timer — ein verspäteter Rejoin aus Raum A kann Raum B nie berühren. (5) **sanitizeName gehärtet:** zusätzlich C1-Controls, Bidi-Steuerzeichen (LRM/RLM/ALM, Embeddings/Overrides, Isolates) und unsichtbare Formatzeichen (Zero-Widths inkl. ZWJ, Word Joiner, Soft Hyphen, BOM, Interlinear) werden entfernt; Cap auf max. 16 sichtbare **Grapheme** via `Intl.Segmenter` (Codepoint-Fallback) plus 48-Unit-Hard-Cap passend zum Rules-Bound; weiterhin ausschließlich `textContent`. Neue Meldungen (`rejoinNoLobby`/`rejoinTab`/`rejoinOk`) vollständig über i18n (DE/EN/TR). (6) **Tests:** Fake-RTDB beider Harnesses spiegelt jetzt die echten v3-players-/p-Rule-Ausdrücke (kein pauschales Erlauben mehr) inkl. Fehlerinjektion (`failWrite`) und `onDisconnect().cancel()`; neue Negativ-Szenarien R3–R10 in der Flow-Suite (Roster-Write-Fehler nach Presence-Gewinn → Rollback ohne Ghost-Seat; Presence-Write-Fehler → kein Teilzustand + Retry; verspäteter Rejoin A→B; fremde id-Switches/Deletes/Mid-Match-Creates; Host-Rejoin im laufenden FFA/1v1/2v2 abgelehnt; Recycling nur Lobby+freie Presence; später Cleanup löscht nie einen fremd übernommenen Record). Suiten: Identity 30→45, Flow 77→107, Rules 90→100; Runner 12/12 grün, E2E-Spike (auf v3-Schema angepasst) + E2E-FFA 3/3 gegen den echten Emulator mit den v3-Rules grün, Goldens 13/13 byte-identisch. (7) `rest_verify_v2.js` → **`rest_verify_v3.js`** (vollständige v3-Matrix inkl. players-Negativfällen, Host-Rejoin-Lobby-Gates, `p/1`-Write-once; weiterhin nur manuell mit `--live`, erst nach dem v3-Publish sinnvoll). Verbleibende dokumentierte Grenze (ohne Auth prinzipbedingt): Presence-Deletes sind offen, und wer id+Raumcode kennt, kann per Same-id-Write den Namen eines Sitzes ändern — Schutz vor Unfällen, nicht vor Sabotage; `auth.uid` rastet später ohne Schema-Bruch ein (2026-07-13)

### Features
- feat: Player Identity, Spielernamen & Lobby-Reconnect (Paket A, **lokal vorbereitet — noch kein Cutover/Deploy**) — Fundament für die Reconnect-Roadmap: (1) **Stabile Browser-Identität ohne Auth** — `ringout_pid` (16-Zeichen-Token aus `crypto.getRandomValues`, in `localStorage` persistiert) identifiziert denselben Spieler über Reload/Tab-Wechsel; per-Tab `onlineTab`-Token als Grundlage des Zwei-Tab-Schutzes (voll in Paket B). (2) **Spielernamen** — `sanitizeName()` (trim, Whitespace-Kollaps, Control-/Bidi-/Unsichtbar-Zeichen raus, max. 16 sichtbare Grapheme — Details/Härtung s. Korrekturrunden-Eintrag oben), persistiert in `ringout_name`, Namensfeld im Online-Dialog + i18n (EN/DE/TR). Namen werden **ausschließlich per `textContent`** gerendert (Lobby-Roster, 1v1-HUD-Cards, Round-/Match-Overlays via `pName`) — nie über `innerHTML`, kein XSS-Vektor. (3) **Neuer Roster-Knoten `players/<seat>={id,name,tab}`** neben dem flüchtigen `p/<seat>` (Presence): `createRoom`/`claimSeat`/`joinRoom` schreiben den Record (Presence bleibt der Write-once-Arbiter; der Record wird vom Seat-Gewinner geschrieben), ein `players`-Listener speist Lobby-/HUD-Namen. (4) **Lobby-Rejoin auf denselben Seat** — `attemptRejoin()` (Host-Seat 0 wie Gast-Seats), ausgelöst per sichtbarer Bestätigung (Join-Tap bei erkannter eigener Identität oder Rejoin-Button aus `ringout_room`, <2 h); Match-Reconnect bleibt Paket B. (5) **Host-Reload schließt die Lobby nicht mehr sofort** — Lobby-Host-Grace (12 s): eine Reload-Lücke bei `p/0` hält den Raum offen (players/0 bleibt bestehen), ein *bewusstes* Host-Leave entfernt zusätzlich `players/0` und schließt sofort (deterministische Unterscheidung über den Roster). Bewusstes Leave bleibt sofortig und räumt `players/<seat>` + `ringout_room` ab. (6) **Protocol lokal auf v3 vorbereitet** (Raum-Schema + Disconnect-Semantik geändert → v2/v3 nicht mischbar); `firebase.rules.json` v3: neuer `players/$i`-Knoten (id `/^[A-Za-z0-9_-]{8,24}$/`, name 1–48 Units, tab, `$other:false`; Garantien in der Korrekturrunde verschärft — s. Eintrag oben), Host-`p/0`-Rejoin nur wenn `players/0/id` existiert (seit der Korrekturrunde zusätzlich nur im Lobby-/Wartezustand), Raum-Erstellung verlangt `players/0`. **Kein Cutover, kein Rules-Deploy, kein Push** — der öffentliche v3-Wechsel erfolgt gebündelt nach Paket B. Bekannte Grenze (dokumentiert): ohne Auth schützt die pid vor Unfällen, nicht vor Sabotage durch jemanden mit dem Raumcode — `players/<seat>/id` ist bewusst so gewählt, dass später `auth.uid` (Anonymous Auth) ohne Schema-Bruch einrastet. Tests: neue **Identity-Suite** (30, sanitizeName/findOwnSeat/genToken/nameForSeat/playerRecord), **Rules-Suite 70→90** (players-Claims, immutable id, Recycling-Gates, Host-/Gast-Rejoin, Name-Grenzen, fremde/ungültige Claims), **Flow-Suite 64→77** (v3-Schema, Name-Sanitization + Live-Roster, Gast-Lobby-Rejoin auf gleichen Seat, Host-Reload-Grace + Rejoin), Race-Suite 115 unverändert grün (Session-Callback-Isolation weiter gedeckt). Fake-RTDB beider Flow-/Race-Harnesses um den `players`-Pfad erweitert. **Runner 12/12 grün, E2E (`test:e2e:ffa`) 3/3 Szenarien gegen den echten Emulator mit v3-Rules bestanden, `git diff --check` sauber.** Physik/Lockstep/Move-Daten byte-unverändert (Goldens 13/13) (2026-07-13)

### Fixes
- fix(P0): Online-FFA-Desync bei 4 Spielern — **Root Cause:** `commit()` wendete den eigenen Move **lokal sofort** an und konnte sogar selbst `beginReveal` auslösen (inkl. Listener-Detach), während `onlineTurnValue` den eigenen Seat übersprang: der Write-once-Arbiter der DB galt für alle **außer dem Autor**. Verlor dessen Commit das Write-once-Race gegen einen **Leave-Sentinel** (ausgelöst durch Presence-Flap: Screen-Lock/App-Wechsel/Netzwechsel feuert `onDisconnect`, und Seats sind nach Matchstart per Rules unwiederbringlich), simulierten die anderen Clients den Sentinel, der Betroffene aber seinen echten Move → divergente Sims (verschiedene Kugeln fallen, verschiedene Sieger/Scores), und sobald Alive-States divergieren, wartet `allAliveCommitted()` clientweise auf unterschiedliche Seats → **Hänger**. **Fix (minimal):** Lockstep-Wahrheit ist ausschließlich die DB — `commit()` sendet online nur noch, der eigene Move wird wie alle anderen über das Turn-Echo angewendet (`onlineTurnValue` verarbeitet jetzt ALLE Seats inkl. `myPlayer`, inkl. Sentinel-Erkennung für den eigenen Seat); `onlineSendCommit` schluckt ein verlorenes Race explizit (`.catch`). Damit ist der Turn-Endzustand eine reine Funktion des DB-Inhalts (reihenfolgeunabhängig), ein geflappter/eingefrorener Client spult die DB-Turns deterministisch nach und sieht sich selbst eliminiert statt zu divergieren — kein Hänger mehr. **Kein Protocol-Bump, keine Rules-/Physikänderung** (Goldens 13/13 bit-identisch). **Warum Tests es nicht sahen:** das `commitMove` der Flow-Suite re-implementierte den alten Lokal-Anwenden-Shortcut und die Fake-DB hatte kein Write-once auf Move-Slots — das Race war dort unmöglich. Beides behoben: Fake-DB-Moves jetzt write-once (wie die echten Rules), `commitMove` spiegelt den Echo-Pfad, **neue F5-Regression** (4 Spieler, Presence-Flap, Sentinel gewinnt Race → identische Commits/gone-Flags/Ejection auf allen 4 Clients inkl. Opfer; Flow-Suite 46→55) **+ F6-Regression** (5 Spieler/alle Seats, unterschiedliche Commit-/Empfangsreihenfolge, zusätzlicher Presence-Flap für Seat 4 → identische Commit-Slots, gone-/alive-Flags, Reveal-Phase, Ballzustände, Score-Zustand und deterministischer `simHash` auf allen 5 Clients, kein Client hängt; Flow-Suite 55→64). **Dev-Diagnose** (`?dev=1`, nur online): deterministischer `[sync]`-State-Hash pro Phasenwechsel + Launch (Bälle/Score/R/seatGone + Commit-Set) — erste abweichende Zeile zweier Clients = Divergenzpunkt; kein Produktions-Logging. Zusätzlich FFA-Round-End-Anzeige korrigiert: nur noch „GRÜN 3" (Gewinnerfarbe + dessen Punktestand), die weiße Gesamt-Score-Reihe entfernt. **Verifiziert (2026-07-10):** automatisiert (Runner 10/10, Flow-Suite 64/64 inkl. 4- und 5-Client-Regression) und lokal mit mehreren simulierten PC-Clients (identischer Spielverlauf/Gewinner/Score). **Live-Validierung mit 4–5 echten Handys inkl. Presence-Flap-Test steht noch aus** (siehe TODO P0) (2026-07-10)

### Features
- feat: echter Rollsound als CC0-Asset — `assets/sfx/marble_roll_loop.m4a` (24 KB, 1,85 s, mono 44,1 kHz AAC): nahtloser Loop aus „Bowling Ball Rolling" von **qubodup** (OpenGameArt.org, **CC0/Public Domain**, laut Autor Aufnahme einer echten rollenden Murmel — Lizenz erlaubt kommerzielle Nutzung + Repo-Redistribution ohne Attribution). Bearbeitung mit portablem ffmpeg (nur Scratchpad, keine Installation): gleichmäßiges Segment 0,6–2,6 s extrahiert, Mono/44,1 kHz, Highpass 45 Hz + Lowpass 10 kHz, **150-ms-Crossfade-Loop** (knackfreier Übergang), Peak −3 dBFS; Quell-/Lizenzdoku in `assets/sfx/README.md`. Der vorhandene Asset-Hook lädt m4a automatisch (Tempo → Gain/Playbackrate, aggregierte Engine, Mute-/Menü-/Replay-Kopplung); nach Hörtest Gain-Kennlinie +6 dB (0,04–0,22 → 0,08–0,40; kein Limiter nötig, Asset-Peak −3 dBFS × 0,40 ≈ 0,28). **Hörprüfung Owner bestanden (2026-07-10):** bei mittlerer Handy-Lautstärke klar hörbar, Murmel-auf-Stein-Charakter, kein Loop-Schnitt/Clipping, still bei Stillstand/Menü/Match-End, Hit-/Launch-/Victory-Sounds bleiben dominant (2026-07-10)
- feat: sichtbares Kugel-Rollen + präsenterer Rollsound (reiner Visual-/Audio-Polish) — (1) **Rollrotation**: die 3D-Kugeln rotieren jetzt sichtbar passend zur Bewegung; pro Frame wird aus dem Render-Delta die Rollachse (up × Bewegungsrichtung) und der Winkel (Weg/BR) berechnet und per Quaternion-Premultiply aufs Mesh akkumuliert — läuft mit der Geschwindigkeit aus, stoppt im Stand, greift nicht beim Fallen (dort bleibt die vorhandene Fall-Rotation), Orientierung wird nur beim Respawn zurückgesetzt; wiederverwendete Temps, keine Allokationen pro Frame; rein visuell, `stepSim`/Simulation byte-unangetastet (Goldens 13/13). (2) **Speckle-Map**: dezente prozedurale 256²-Canvas-Textur (helle Basis, weiche dunkle Sprenkel + Lichtflecken), geteilt über alle 5 Ball-Materialien via `map` — multipliziert mit der Materialfarbe, Grundton/Marmel-Look bleibt; ohne Oberflächendetail wäre die Rotation auf den uniformen Hochglanzkugeln unsichtbar gewesen (einzige Material-Ergänzung, kein Umbau). (3) **Rollsound: prozedurale Ansätze deaktiviert, Asset-Support vorbereitet** — das alte per-Ball-Loop-Rauschen (klang wie Wind) und ein zweiter prozeduraler Versuch (Low-Rumble + Stein-Ticks) wurden nach Owner-Review beide verworfen: lieber kein Rollsound als ein schwacher Fake. `rollUpdate` lädt jetzt lazy ein **optionales Asset `assets/sfx/marble_roll_loop.*`** in browserfreundlicher Format-Präferenz **m4a → mp3 → ogg → wav** (m4a läuft auch auf älteren iPhones/Safari, ogg nicht; kurzer nahtloser Loop „Murmel rollt auf Stein/Marmor", 0,5–2 s, lizenzfrei — noch nicht vorhanden): existiert eine Datei, spielt eine einzelne aggregierte Loop-Engine (Tempo → Gain quadratisch + leichte Playbackrate-Färbung, weiche Fades, aus bei Stillstand/Mute/Menü/Replay/Match-End, Mobile-Gesten-Unlock, persistente Nodes ohne Leak); fehlt alles, bleibt es **bewusst still** — jede Quelle wird pro Seitenladen genau einmal probiert (404/Decode-Fehler → nächstes Format, kein Fetch-Spam, kein Fehler, kein Fallback-Rauschen). Kein externes Asset ohne Freigabe eingebaut. `?r2d=1`: kein 3D-Rollen (2D-Pfad unverändert), Rollsound wie bisher. Keine Physik-/Gameplay-/Firebase-/Protocol-/Kamera-Änderung. Runner 10/10 grün. **Sichtprüfung Owner bestanden (2026-07-10):** sichtbares Rollen + Speckle-Map akzeptiert, bewusste Stille ohne Asset akzeptiert, Bot 1v1/2v2 Duo, FFA, Mute, Menü still, `?r2d=1`, Mobile-Performance (2026-07-10)

### Fixes
- fix: Kamera-Drag-Richtung natürlich (Grab-Metapher) — Wischen/Ziehen auf der 3D-Arena fühlte sich horizontal invertiert an: `camYawT+=dx` bewegte bei Rechts-Wisch die Kamera nach rechts, wodurch die Arena scheinbar nach links rotierte. Ein-Vorzeichen-Fix (`camYawT-=dx`): die Arena folgt jetzt der Wischrichtung wie beim direkten Anfassen; vertikal war das Verhalten bereits trackball-natürlich und bleibt unverändert. Betrifft Touch UND Maus (gleicher Pointer-Pfad). Slingshot/Aim unangetastet — `cam.rotate` setzt nur das Gesten-Ziel, `r3dCamMath` wird der Kamera wie bisher exakt nachgeführt (Mapping-Suite 48/48 unverändert), `viewAngle`/Per-Seat-View separat. Keine Physik-/Gameplay-/Firebase-/Protocol-Änderung. **Sichtprüfung Owner bestanden (2026-07-10):** Wischrichtung natürlich, Pinch/Doppeltipp ok, Slingshot/Aim auch nach Drehung korrekt, Bot + Online/FFA geprüft (2026-07-10)

### Features
- feat: Round-End-/Match-End-/Feedback-Premium-Polish (ikonisch, textarm) — die emotionalen Spielmomente als klare Premium-Inszenierung statt textlastiger Dialoge: (1) **Round-End, modus-abhängig**: *FFA* → cineastische Center-Einblendung ohne Panel-Kasten (kompakte Farb-Headline mit Glow „GREEN 1" / „BLAU 2" / „MAVİ 1" — Farbe + Punktestand des Gewinners, kein „SCORES/PUNKTET"; Gold-Ornamentlinie, Gesamtscore, Mini-Zeile „ROUND n", radiale Fokus-Abdunklung, Scale/Blur/Fade-Entrance, auto-hide 1,6 s) — dort müssen mehrere Spieler klar sehen, wer punktet; *1v1/2v2 Duo* → **Score-Fly-Animation statt Panel**: nur der neue Score („1 : 0", kein Farbwort, kein Name — die Gewinnerfarbe spricht ausschließlich über Textfarbe/Glow) erscheint groß zentral, hält kurz und schwebt dann verkleinernd zur Scorebox, die beim Eintreffen gold pulst (JS-getriebene CSS-Transitions, Ziel per `getBoundingClientRect`; bei `prefers-reduced-motion` nur Fade ohne Flug) — Spielanimation statt Dialog, kein Satz, keine Panel-Fläche, Spielfluss ununterbrochen. Beide ersetzen den Runden-Toast, `startRound()` läuft ungebremst. (2) **Match-End**: große ikonische Headline — goldenes „VICTORY" beim eigenen Sieg, gedämpftes „DEFEAT" bei Niederlage (dann klein „<FARBE> WINS" in Teamfarbe), lokal (dev) neutral „<FARBE> WINS"; darunter nur „FINAL SCORE · x : y"; Panel Navy-Glass mit Gold-Rand, Rematch prominent (Gold-Bevel), Replay/Menü sekundär (Dark-Glass); Trophy nur bei Sieg; Disconnect-Overlay (`onOppLeft`) stellt wt/Header korrekt zurück. (3) **RINGOUT**: kräftiger Gold-Pop mit Overshoot-Keyframe beim entscheidenden Rauswurf (Phasen-Watcher in `loop()`, **nicht** in `stepSim`). (4) **Score-Pulse**: Gold-Ring + sanfter Scale-Bump am Score-Panel. (5) i18n EN/DE/TR: Farbnamen col0–col4, VICTORY/DEFEAT/WINS/FINAL SCORE/ROUND/SCORES/RINGOUT, Spielernamen (`pName`: YOU/OPPONENT/Player usw.). `prefers-reduced-motion` reduziert alle Animationen. Hit-Flash bei Kollisionen bewusst verschoben (3D hat dezente Kontakt-FX; Screen-Effekte wären Risiko fürs Aim-Gefühl). Physik/Lockstep/Scoring/Protocol byte-unverändert (`stepSim` unangetastet, Goldens 13/13); `tools/test_ffa.js`-Env um 1-Zeilen-`showRoundEnd`-Stub ergänzt. Runner 10/10 grün. **Sichtprüfung Owner bestanden (2026-07-10):** Score-Fly 1v1/2v2 (nur Score in Gewinnerfarbe), FFA kompakt („GRÜN 1"), Match-End unverändert, Bot 1v1 + 2v2 Duo, Mobile Portrait, `?r2d=1` (2026-07-10)
- feat: Premium-Boot-Screen + Menü-Fade-in (Mobile Load Polish) — beim ersten Öffnen erschien das Menü halbfertig (Fallback-Hintergrund), weil die echte 3D-Arena (GLB ~1,5–2 s mobil + HDRI) asynchron nachlädt und dann sichtbar „reinploppte". Jetzt: reines HTML/CSS-**Boot-Overlay** ab dem ersten Paint (Navy-Verlauf, RINGOUT-Logo, goldene Shimmer-Line als indeterminate Loader, Loading-Text über i18n EN/DE/TR); das Menü ist währenddessen `visibility:hidden` (kein früher Backdrop-Blur-Jank) und **faded erst ein, wenn der erste echte 3D-Frame gerendert wurde** (neue First-Frame-Condition in `loop()`: erster Tick mit `r3dActive` → idempotentes `finishBoot()`). Fallbacks: `?r2d=1` → sofort Menü ohne 3D-Warten; `initR3D`-Fehler → Loader weg + 2D-Menü (bestehender Toast bleibt); 10-s-Sicherheits-Timeout → nie im Loader hängen. `prefers-reduced-motion` stoppt die Shimmer-Animation. Keine Gameplay-/Physik-/Firebase-/Protocol-/Kamera-/Renderer-Änderung, keine Assets. Runner 10/10 grün. **Sichtprüfung Owner bestanden (2026-07-10):** Start sauber, kein halbfertiges Menü, Loader sofort, Arena-Fade nach ~1–2 s, Bot 1v1, Versus-Modal, `?r2d=1`, Sprachen (2026-07-10)
- feat: In-Game-UI auf Premium-Menü-Niveau (nur CSS, Luxury-Mobile-Stil) — das komplette In-Game-HUD nutzt jetzt die Designsprache des Premium-Menüs (Navy-Glass, feine Gold-Akzente, weiche Tiefe, ruhig statt Arcade): **Player-Cards** als Glass-Panels mit teamfarbenem Soft-Glow im Aktiv-Zustand (statt roher 2px-Rahmen), **Score-Panel** mit gold-getöntem Rand, größerem Score und eleganter „RUNDE"-Zeile, **Hinweistext** als Premium-Hint-Chip jetzt in 2D UND 3D (vorher nur 3D), **„Stehen bleiben"** im Gold-Bevel-Look der Menü-CTA, **Reset-Button** als Dark-Glass-Utility, **☰/🔊-Buttons** rund in der `.mico`-Formensprache des Menüs, **FFA-Chips** und **Toast** angeglichen. Rein visuell: kein HTML/JS angefasst (`updateHud()` überschreibt `className` — Styling liegt vollständig auf den bestehenden Klassen), keine Gameplay-/Online-/Kamera-/Physik-/Sound-Änderung. Runner 10/10 grün. **Sichtprüfung Owner bestanden (2026-07-10):** HUD, Score/Runde, Hint-Chip, Primary-/Utility-Buttons, FFA-Chips, Toasts, Mobile Portrait, Bot 1v1, Online 1v1/FFA, `?r2d=1` (2026-07-10)
- feat: ONLINE VERSUS mit Format-Modal + Bot-Format-Modal — Korrektur einer irreführenden Bezeichnung: „ONLINE 2V2" suggerierte 4 echte Spieler, tatsächlich sind es **2 echte Spieler mit je 2 Kugeln**. Die Menü-Karte heißt jetzt **ONLINE VERSUS** („1v1 OR 2v2 · 2 PLAYERS"); die CTA öffnet ein Premium-Modal (Glass/Navy/Gold, große Touch-Karten `.vopt`) mit **1v1 DUEL** (2 PLAYERS · 1 BALL EACH → `fmt='single'`) und **2v2 DUO DUEL** (2 PLAYERS · 2 BALLS EACH → `fmt='double'`) — danach der bestehende Online-Dialog (Create/Join/Back, optisch angeglichen: Gold-Bevel-Buttons, Placeholder „ENTER CODE"). Online-1v1 ist damit wieder ohne `?dev=1` erstellbar. **Bot-CTA öffnet analog ein Modal**: 1v1 VS BOT / 2v2 DUO VS BOT — kein „Coming Soon" nötig, `botMove()` routet seit jeher fmt-abhängig auf die validierte `bot1v1`/`bot2v2`-Heuristik (im alten Menü war Bot-2v2 über die Format-Pills spielbar). Bewusst NICHT „Team Battle" (reserviert für einen echten 4-Spieler-Modus). Alle neuen Texte über das I18N-System (EN/DE/TR), FFA-Flow unverändert (eigener Hauptmodus, kein Versus-Modal), keine 4-Spieler-Logik, keine Firebase-/Protocol-/Physikänderung. Runner 10/10 grün (2026-07-10)
- feat: Premium-Hauptmenü (Playtest-UX-Redesign nach Owner-Referenzbild) — komplettes Menü-Redesign auf Premium-Mobile-Game-Niveau: Sky-Backdrop (mehrschichtige Gradients mit warmem Gold-Glow), „RINGOUT"-Titel mit Gold-Ornament-Linie, **echte 3D-Arena als Hero** (das Menü ist im 3D-Modus transparent über der live gerenderten Szene — HDRI-Himmel, Marmor-GLB-Plattform mit Schwebe-Bob, driftende Wolken, rotierender Goldring; nur dezente Lese-Verläufe oben/unten. Eigener Menü-Framing-Zweig in `frame()`: Plattform groß, Zentrum bei ~37 % Bildhöhe, `baseDist×0.87` — Gameplay-Framing und `r3dCamMath` unverändert, Mapping-Suite 48/48. Kugel-Preview je Modus über den ECHTEN Spawner `placeBalls()`: FFA 5 Kugeln blau/rot/grün/gelb/schwarz, 2v2 2 blaue vs. 2 rote, Bot 1v1 — Kartenwahl setzt Pre-Game-State wie früher die Menü-Pills. Ein zuvor umgesetzter Fake-Canvas-Fidelity-Pass wurde nach Owner-Review verworfen und entfernt; `?r2d=1`/3D-Fehler fallen auf einen ruhigen Verlaufs-Hintergrund ohne Fake-Arena zurück), **drei Modus-Karten** (ONLINE FFA · 2–5 PLAYERS / ONLINE 2V2 · 4 PLAYERS · 2 vs 2 · HOST STARTS / BOT TRAINING · VS AI · SOLO PRACTICE) mit Inline-SVG-Icons, Gold-Glow-Active-State und „ACTIVE"-Chip, **eine goldene CTA** mit modusabhängigem Label (PLAY ONLINE FFA / PLAY ONLINE 2V2 / START BOT TRAINING) + Info-Chip, Sound-Toggle oben (synchron mit In-Game-Button). **Online-FFA ist Default-Auswahl und Flaggschiff-Modus.** CTA verdrahtet auf bestehende Flows: FFA → `mode='ffa'` + openOnline (menuMode unangetastet, onBack stellt `mode=menuMode` wieder her), 2v2 → `mode='online'`+`fmt='double'` + openOnline, Bot → `fmt='single'` + Spielstart. **Ausgeblendet statt gelöscht** (`?dev=1` zeigt das Legacy-Panel): Hotseat-2-Spieler, FFA lokal inkl. 2/3/4/5-Auswahl, Bot-Stufen Leicht/Mittel, Format-Auswahl (Online-1v1-Erstellung), Runden-bis-Sieg-Auswahl; Regeln-Textbox vom Home-Screen entfernt (In-Game-Hinweise erklären die Steuerung). **Bot ist hard-only** (`diff='hard'` Default) und risikoarm verstärkt: feinere Winkel-Kandidaten (±0.08 → 30 statt 22) + enge Auswahl-Toleranz (0.2 statt 0.6) — kein Cheat, keine Physik-Konstante geändert, 2v2-Bot unverändert. Keine Firebase-/Rules-/Protocol-Änderung, Goldens unangetastet, Online-Logik unverändert. Runner 10/10 grün, Testzahlen unverändert. **Sichtprüfung Owner bestanden (2026-07-10):** echtes 3D-Menü, ONLINE VERSUS, 1v1 DUEL + 2v2 DUO DUEL spielbar, Bot 1v1 + 2v2 Duo, Online-Dialog, FFA unverändert, Sprachen, Sound, `?dev=1`, `?r2d=1`, Mobile Portrait, In-Game-Kamera unverändert (2026-07-09/10)
- feat: v1-Localization (EN/DE/TR) + funktionales Settings-Modal — ⚙-Button im Hauptmenü öffnet ein Settings-Modal (bestehender Premium-Overlay-Stil) mit Sprachwahl English/Deutsch/Türkçe; Auswahl in `localStorage` (`ringout_lang`) persistiert, Default English, Umschalten live ohne Reload (`applyLang()`). Zentrales `I18N`-Dictionary + `T(key)`-Helfer statt verstreuter Strings. Lokalisiert: alle Hauptmenü-Texte (Karten, Subtitles, CTA, Info-Chip, Tagline, ACTIVE-Chip), Settings-Modal, Online-Dialog-Grundtexte (Intro je Modus, Raum erstellen/Beitreten/Zurück, DEIN CODE, Warte-Texte, LOBBY/Start), Leave-Bestätigung, End-Overlay-Buttons (Rematch/Replay/Menü), Online-Statusmeldungen in openOnline/createRoom/joinRoom (Erstelle Raum…, Code-Teilen, Trete bei…, Verbunden!, Raum nicht gefunden/voll, Firebase-Fehler) sowie der Reset-Toast. **Bewusst NICHT lokalisiert (v1):** testgebundene Texte in extrahierten Funktionen (validateRoom-Reasons, renderLobby-Hints, startFfaMatch-/Leave-Toasts, onLobbyClosed, onOppLeft — von Test-Suiten hart asserted) und In-Game-Texte (HUD, Phase, Cover, Overlays-Inhalte). Flow-Suite-Env um 1-Zeilen-`T`-Stub ergänzt (Identität; keine Asserts auf lokalisierte Texte). 2v2-Karte korrigiert: „STARTS WHEN FULL" statt „HOST STARTS" (2v2 startet automatisch bei vollem Raum). Runner 10/10 grün (2026-07-09)

### Sicherheit / Kosten
- fix: Room-Cleanup v1 — leere Firebase-Räume werden jetzt automatisch entfernt, statt dauerhaft (inkl. Move-Historie) liegenzubleiben. `firebase.rules.json` erlaubt **additiv** das Löschen eines ganzen Raums, aber **nur** wenn kein Seat `p/0…p/4` mehr präsent ist (`data.exists() && !newData.exists() && …!== true`); Create-Regel, Überschreib-Verbot und das gesamte v2-Schema bleiben unverändert. `leaveOnline()` entfernt nach der eigenen Presence best-effort den ganzen Raum (`remove(rRef())` mit `.catch`) — sind andere Seats besetzt, lehnt die Rule den Delete still ab, sodass **laufende 1v1/2v2/FFA-Matches unberührt** bleiben und der letzte sauber gehende Spieler den Raum abräumt. **Kein Protocol-Bump** (`ONLINE_PROTOCOL_VERSION` bleibt 2, rein additive Berechtigung, abwärtskompatibel), keine Gameplay-/Physikänderung. Verifikation: lokaler Runner 10/10 grün (Rules-Suite 59→70, +11 Delete-Fälle: leer erlaubt, je p/0–p/4 präsent blockiert, „playing" mit Seats blockiert, Overwrite/Non-Existent verboten), Fake-RTDB-Stub in `test_ffa_flow.js` spiegelt die Cleanup-Rule; **Live-REST-Verify nach Publish 67/67 grün** (Delete bei präsenten Seats abgelehnt, Delete bei leerem Raum erlaubt, Test-Räume automatisch abgeräumt). Residual: Crash/Tab-Close des letzten Spielers kann eine winzige leere Hülle hinterlassen (kein Move-Wachstum) — vollautomatischer Sweep bräuchte Cloud Functions/Blaze (bewusst verschoben) (2026-07-09)

### Tests
- test: CI-Extraktion line-ending-robust gemacht — auf Linux/LF (GitHub Actions) schlugen `ValidateRoom` und `FFA-Online-Prep` mit `SyntaxError: Unexpected token ';'` fehl, während lokal auf Windows/CRLF alles grün war. Ursache: aus `index.html` extrahierte Zeilen wie `const GEN_MAX=10000;   // …` enthalten einen Trailing-`//`-Kommentar; beim Zusammenbau mit `;` beendete auf Windows nur das mitgefangene CRLF-`\r` den Kommentar, auf LF fehlte es → der Kommentar verschluckte das folgende `function validateRoom…`. Neuer gemeinsamer Extractor `tools/extract.js` (`loadIndexHtml()` normalisiert alle Zeilenenden auf `\n`, `grab()`); beide betroffenen Suiten nutzen ihn jetzt und fügen Snippets ausschließlich per Zeilenumbruch statt `;` zusammen. Die newline-getrennten Suiten (`test_ffa`, `test_physics_golden`, `test_lockstep`, `test_ffa_flow`) waren nie betroffen und bleiben unverändert. Lokaler Runner nach dem Fix 10/10 grün; da der Loader auf LF normalisiert, testet der lokale Lauf jetzt exakt den CI-Pfad. Keine index.html-/Physik-/Rules-/Asset-Änderung (2026-07-09)
- ci: GitHub-Actions-Workflow `.github/workflows/tests.yml` führt bei jedem `push`, `pull_request` und manuell (`workflow_dispatch`) automatisch `node tools/run_all_tests.js` aus — checkt Repository aus, richtet Node 20 ein und lässt die 10 Offline-Suiten laufen. **Kein `npm install`** (die Suiten nutzen ausschließlich Node-Built-ins `fs`/`path`/`os`/`child_process`, es gibt keine `package.json`/Lockfile). CI ist ein **reiner Sicherheitscheck** — kein Build, kein Deployment, kein Firebase-Zugriff. **Live-REST-Verify bleibt bewusst manuell** und läuft nie in CI (`tools/rest_verify_v2.js` schreibt in die Live-DB, nur mit `--live`). Lokaler Runner vor dem Commit grün (10/10). Keine Code-/Physik-/Rules-/Asset-Änderung (2026-07-09)
- test: vollständige lokale Test-Batterie ins Repo gerettet + zentraler Runner — die zuvor nur im flüchtigen Scratchpad liegenden Suiten sind jetzt versioniert unter `tools/`: `test_syntax.js`, `test_sanitize.js`, `test_validateroom.js`, `test_lockstep.js`, `test_ffa.js`, `test_ffa_online.js`, `test_ffa_flow.js`, `test_rules.js` sowie das Live-Skript `rest_verify_v2.js`. Alle Pfade sind repo-relativ (`path.join(__dirname, '..', …)`, Konvention wie `test_physics_golden.js`), `index.html` wird nur read-only extrahiert; `test_syntax.js` legt seine temporären `.mjs`-Blöcke jetzt in `os.tmpdir()` statt neben sich ab. Neuer **zentraler Runner `tools/run_all_tests.js`** führt alle 10 Offline-Suiten als Kindprozesse aus, druckt pro Suite genau eine kompakte Zeile, gibt bei Fehlschlag die volle Ausgabe der betroffenen Suite aus und liefert Exit-Code 1, sobald eine Suite fehlschlägt (sonst 0). **Live-REST-Verify bleibt bewusst manuell:** `rest_verify_v2.js` läuft nicht im Runner und bricht ohne `--live`-Pflichtflag sofort mit Exit 2 ab, ohne die Datenbank zu berühren. Verifikation: alle Suiten einzeln und über den Runner grün (Syntax OK, Golden 13/13, Mapping 48/48, Sanitize 19/19, ValidateRoom 40/40, Lockstep 24/24, FFA-Kern 18/18, FFA-Online-Prep 40/40, FFA-Flow 46/46, Rules 59/59), Fehlererkennung des Runners mit Wegwerf-Suite bestätigt (FAIL + Exit 1), `--live`-Sperre bestätigt. CI bleibt ein separater Folge-Task. Keine Code-/Physik-/Rules-/Asset-Änderung (2026-07-09)
- test: Online-FFA-Live-Smoke bestanden (M8-T3e, akzeptiert) — auf https://ringoutgame.github.io/ringout/ mit 5 echten Spielern verifiziert: Lobby + Host-Start, alle Joins, Per-Seat-Ansicht (eigene Kugel vorne/unten), Last-Man-Standing, stabiles Match; 1v1/2v2 weiterhin stabil. **RingOut ist bereit für den kleinen privaten Playtest inkl. Online-FFA.** Bekannte v1-Grenzen bleiben: kein Reconnect, Sitzlücken blockieren den Lobby-Start, Rematch-„Geist" bei verlassenen Spielern möglich, noch kein öffentlicher Launch (Auth/App Check/Room-TTL ausstehend). Keine Code-/Firebase-Änderung (2026-07-09)

### Features
- feat: Online-FFA 2–5 Spieler aktiviert (M8-T3c inkl. Nachbesserung, akzeptiert) — die vier Blocker sind durch echte Flows ersetzt: Host erstellt ffa-Räume (`state:'lobby'`), Gäste joinen per Code über `claimSeat` (Write-once-Race, niedrigster freier Seat 1–4), **Live-Lobby** (Presence-Roster mit PCOLS-Kugeln, eigene markiert, n/5, Host-Start ab 2, Gäste „Warte auf Host…"). **Start-Gate statt Auto-Nachrücken:** Start nur bei lückenlos belegten Seats 0..n−1 (sonst „Warte auf freien Sitz / Spieler soll neu beitreten."); Host schreibt sequenziell `state:'playing'` → `seats:n` (kein Multi-Path-Update — die seats-Rule sieht sonst den neuen state nicht); ein einziger `seats`-Listener ist das synchrone Startsignal für Host und Gäste (Rules garantieren seats erst nach state); Claim-Race nach Host-Kopfzählung wird client-seitig sauber ausgeworfen (`myPlayer>=seats`). **Per-Seat-Ansicht `viewAngle()`:** jeder Seat 0–4 sieht die eigene Kugel vorne/unten (Rotation −seat·2π/N konsistent über Input `localPt`, 2D-Canvas, Kugel-/Power-Labels gegenrotiert, 3D-Kamera-Azimut); 1v1/2v2-Spiegelung exakt erhalten (P1 = π, exakter `2*cx-x`-Pfad), lokaler FFA unverändert (offline 0). **Last-Man-Standing online** über den bestehenden N-Spieler-Lockstep (`allAliveCommitted`, Eliminierte zuschauen mit Aim-Sperre „Du bist raus"). **FFA-Leave im Match = Elimination statt Match-Abbruch:** verbleibende Clients füllen den offenen Move-Slot des Verlassenen mit einem Stand-still-Sentinel (`idx!==seat`; Write-once-Rule als Schiedsrichter, echter Commit gewinnt) — alle Clients spielen mit identischen Moves aus der DB (deterministisch, kein Deadlock); am Sentinel erkennen alle den Leave und setzen die Kugel bei `beginReveal` hinter die Ringkante → normaler Ring-Out beendet Runde/Match korrekt (Toast „Spieler X hat das Match verlassen.", 2er-FFA hängt nicht, Host-Leave im Match ok, Rematch ab 2 Verbundenen). **1v1/2v2-Disconnect-Verhalten unverändert** (Leave beendet das Match); Lobby: Gast-Leave aktualisiert Roster, Host-Leave bricht für Gäste sauber ab; `mode`/`ffaN`-Menü-Restore nach Online. Keine Firebase-/Rules-Änderung, kein Protocol-Bump (v2), keine Physikänderung, kein stepSim-Eingriff, Goldens unangetastet. Tests: Golden 13/13 bit-exakt, Mapping 48/48 (localPt-Pin auf viewAngle-Kontrakt + 17 neue Checks „eigene Kugel unten" für N=2–5), Bestand 83/83, FFA 18/18, FFA-Online-Prep 40/40 (+14 Start-Gate), **neue Multi-Client-Flow-Suite 46/46** (Fake-RTDB mit v2-Rule-Verhalten: Lobby/Start/Lockstep mit 2–5 Clients, Leave-Elimination, Disconnects, Rematch, 1v1-Regression), Rules-Suite 59/59. Sichtprüfung Owner bestanden: 3/5 Spieler mit Per-Seat-Ansicht, Leave-Flows, 1v1/2v2-Regression inkl. Gast-Spiegelung, lokaler FFA, 3D-Default, `?r2d=1` (2026-07-09)

### Chore
- chore: Protocol-v2-Cutover für Online-FFA (M8-T3b, akzeptiert) — `ONLINE_PROTOCOL_VERSION` 1 → 2 zusammen mit der passenden `firebase.rules.json`-Erweiterung, **veröffentlicht und live REST-verifiziert (56/56 Checks)**: `config.fmt` erlaubt zusätzlich `'ffa'`; ffa-Räume nur mit `state:'lobby'` erstellbar (single/double weiterhin ohne `state`/`seats`); Einweg-Übergang `lobby→playing` nur mit mindestens einem Gast (`p/1`); `seats` (2–5) write-once nach dem Start; Seat-Claims `p/0`–`p/4` write-once und nur solange die Lobby offen ist (kein Join nach Start, Presence-Delete bleibt erlaubt); Moves für Seats 0–4 und `idx` 4 nur in ffa-Räumen. **Jede neue Bedingung ist auf `fmt==='ffa'` gegated — single/double-Validierung verhaltensidentisch** (neue lokale Rules-Engine-Suite 59/59 wertet die echten Rule-Strings gegen Write-Szenarien aus, Sensitivität per manipulierten Kopien bewiesen; REST-Regression bestätigt u. a. unverschärftes `p/1`). Online-FFA bleibt clientseitig **weiterhin deaktiviert** (vier Blocker aktiv). Live-Smoke auf Protocol v2 bestanden: 1v1, 2v2, Commit/Reveal, Rematch, Leave/Disconnect, 3D-Default, `?r2d=1`. Drei REST-Testräume verbleiben in der DB (7SNX, DDKU, 5CZ4 — Rules erlauben kein Löschen, später manuell in der Console entfernen). Ein späterer Physik-Tuning-Pass (M5-T2) würde auf v3 bumpen. Keine Physikänderung, Goldens unangetastet (2026-07-09)

### Features
- feat: Client-Vorbereitung Online-FFA (M8-T3a, akzeptiert) — Online-FFA bleibt **bewusst deaktiviert** (vier Blocker: FFA-Menü-Button, `createRoom`, `joinRoom`, Lobby-Start → Toast „Online-FFA kommt im nächsten Schritt."), aber der Client ist vorbereitet: `validateRoom` akzeptiert strukturell `fmt:'ffa'` (nur `state:'lobby'` + freier Seat 1–4; single/double-Pfad verhaltensidentisch), versteckte Lobby-UI mit Roster-Renderer (Seats 0–4 als PCOLS-Kugeln, Zähler n/5, Host-Start ab 2, Gäste „Warte auf Host…"), Seat-Claiming vorbereitet (`pickFreeSeat` + `claimSeat` mit Write-once-Race-Retry, noch unbenutzt), N-Spieler-Reveal-Helper `allAliveCommitted()` (Eliminierte zählen nicht) ersetzt beide `aimSet[0]&&aimSet[1]`-Stellen, Presence-/Turn-Listener von `opp=1-myPlayer` auf Seat-Schleifen generalisiert (für 2 Spieler äquivalent). Firebase-Rules, `ONLINE_PROTOCOL_VERSION` (=1, TODO-Kommentar für T3b-Bump) und Physik unverändert; golden_physics.json unangetastet. Neue Suite FFA-Online-Prep 26/26; Online 1v1/2v2 im Zwei-Tab-Test manuell geprüft und funktionsfähig (2026-07-09)
- feat: lokaler Free-for-All-Kern für 2–5 Spieler (M8-T2, akzeptiert) — neuer Hotseat-Modus „👥 FFA" (Spieleranzahl 2/3/4/5 wählbar): jeder Spieler eine Kugel, gleichmäßig im Kreis platziert, verdecktes Zielen reihum über den Cover-Screen, dann gleichzeitiger Schuss. **Last-Man-Standing-Rundenlogik:** Eliminierte bleiben draußen und werden beim Zielen übersprungen, die Runde läuft über mehrere Züge, bis nur ein Spieler übrig ist; Gleichzeitig-Out-Tiebreak deterministisch (am wenigsten weit draußen gewinnt); Matchsieg bei `winTarget` Rundensiegen (Default 3). **Zentrale Spielerfarben-Tabelle `PCOLS`** (Slots 0–4: Blau/Rot/Grün/Gelb/Schwarz; Slots 0/1 exakt die bisherigen Werte, schwarze Kugel mit silbernem Rim/Glow) speist 2D-Kugeln, Partikel, Slingshot/Pfeile, HUD, Cover und die 3D-Materialien (jetzt 5 statt 2). Kompaktes FFA-HUD als Chip-Leiste (Farbpunkt + Score, aktiver Spieler markiert, Eliminierte ausgegraut). `stepSim`-Rundenende verallgemeinert („≤1 Spieler mit lebenden Kugeln") — für 2 Spieler nachweislich bit-identisch (Golden 13/13 ohne `--update`; Harness extrahiert nur zusätzlich die `PCOLS`-Zeile). Der lokale FFA-Kern ist die technische Basis/der Testharness für den späteren Online-FFA (M8-T3+, noch offen). Kein Online-FFA, keine Firebase-Änderung, kein Protocol-Bump, keine Physikwerte geändert. Neue FFA-Logik-Suite 18/18 (3/5 Spieler, Eliminierung über mehrere Züge, Tiebreak, Runden-/Matchsieg, 2-Spieler-Äquivalenz); Regression manuell bestätigt (Bot, PvP 1v1/2v2, Online-Zwei-Tab, 3D + `?r2d=1`) (2026-07-08)

### Fixes
- fix: Online-Leave-/Disconnect-UX verbessert (M7-T1a, akzeptiert) — Vorbereitung auf den privaten Playtest: (1) Gegner-Disconnect zeigt beim verbleibenden Spieler jetzt ein Overlay „Gegner hat den Raum verlassen." mit ↩-Menü-Rückweg (Rematch/Replay ausgeblendet, 🔌 statt 🏆) statt nur eines 2,2-s-Toasts — kein Hängenbleiben in der Sackgasse; `gameOver()` stellt das normale Sieg-Overlay wieder her. (2) ☰-Button fragt bei laufendem Online-Match jetzt per Bestätigungs-Overlay „Online-Match verlassen?" (Abbrechen/Match verlassen) nach, statt still den Raum zu verlassen; „Match verlassen" nimmt denselben sauberen Leave-Pfad wie ein Disconnect (`leaveOnline()`: Presence-Remove → Gegner-Overlay), „Abbrechen" berührt Firebase nicht. Alle Exit-Pfade (Tab schließen, ☰, End-Overlay-Menü) laufen einheitlich über Presence-Remove. Kein Protocol-Bump, keine Firebase-Änderung, keine Physik-/Sound-/Grafikänderung; 3D und `?r2d=1` verifiziert (2026-07-08)

### Features
- feat: Offline-Physics-Tuning-Harness (M5-T1, akzeptiert) — URL-Flag `?tune=br:28,fend:0.9895,stopv:0.12` (validiert & geklemmt: br 20–40 ‰, fend 0.980–0.992, stopv 0.05–0.20) für lokale Sizing-/Ice-Feel-Playtests mit sichtbarem TUNE-Badge; wirkt über `curFE()`/neuen `curST()`-Wrapper/BR-Override. Online hart blockiert bei aktivem Tune (`openOnline`/`createRoom`/`joinRoom` → Toast „Tuning aktiv — Online deaktiviert") — getunte Werte erreichen Lockstep/Firebase nie. Default ohne Flag bit-identisch (Golden 13/13 ohne `--update`, nur `curST`-Stub in der Test-Env). Neu `tools/tune_compare.js`: 5 Szenarien Ist- vs. Tune-Werte über die echte extrahierte Sim (Frames, Weg, Auslauf nach SLOWV, Ringout). Keine finalen Physikwerte, kein Protocol-Bump, keine Firebase-Änderung (2026-07-08)

### Fixes
- fix: 3D-Aim-Zuverlässigkeit + Overlay-Clipping — Aim-Start prüft jetzt eine Screen-Space-Zone am projizierten Kugelmittelpunkt (mind. 52 CSS-px bzw. Silhouette×1.4, nächste eigene Kugel gewinnt) statt Welt-Distanz am Ray-Bodenpunkt, der bei flacher Kamera/Zoom/Randlage die sichtbare Kugel verfehlte; Zug jetzt relativ zum Griffpunkt (kein Phantom-Offset beim Antippen). `#cv`-Overlay ist im 3D-Modus viewport-groß — Aim-Pfeil und Prozentzahl werden an der Arena-Außenkante nicht mehr abgeschnitten; Principal Point bleibt am Wrap-Zentrum verankert, UI-Buttons behalten per z-index Vorrang, Drall-Empfindlichkeit unverändert am Wrap-Quadrat kalibriert, Kamera-Drag funktioniert jetzt auch außerhalb des Arena-Quadrats. 2D ohne Flag pixel-identisch (2026-07-08)

### Features
- feat: 3D-Renderer ist jetzt Standard (M4-T4, akzeptiert) — `R3D_WANTED` invertiert: ohne URL-Flag lädt der 3D-Modus; `?r2d=1` erzwingt den unveränderten 2D-Pfad; `?r3d=1` bleibt kompatibel (No-Op, alte Links funktionieren). Fallback unverändert: jeder 3D-Fehler (CDN/three, WebGL, HDRI, GLB) → Toast + 2D-Modus. `?perf=1` funktioniert in beiden Modi (in 2D ohne GLB-/HDRI-Zeilen). Einzeiler-Änderung + Kommentar-Updates; kein Protocol-Bump, keine Firebase-/Physik-/Sound-/Designänderung (Golden 13/13, Mapping 31/31, Bestand 82/82). Sichtprüfung bestanden: Default 3D, `?r2d=1` 2D, Handy Portrait, Bot-Runde, Kamera/Zielen/Ringout (2026-07-08)
- feat: Audio & Ball Feel Polish (M4-T3, akzeptiert) — WebAudio-Sound-Pass ohne Assets: Murmel-Kollisions-Klick via Modal-Synthese (Kontakt-Transient + inharmonische Teiltöne + tiefer Körper; Aufprallstärke koppelt Lautstärke/Helligkeit/Tonhöhe, leichte Zufalls-Verstimmung pro Hit), Roll-Sound pro Ball (wiederverwendete Loop-Rausch-Voices, Geschwindigkeit steuert Gain/Filter, keine Allokationen pro Frame, still bei Stillstand/Mute/Replay), Lade-/Zieh-Sound als dezentes tiefes „Strain"-Rauschen statt Sirenen-Ton, Ringout = Kanten-Kontakt + leiser Wind (Gliss-„Plopp" entfernt), dezenter Wind-Drop für nicht-entscheidende Falls. Spam-Schutz (70 ms Paar-Cooldown, 30 ms global, max. 8 Hit-Voices, Minimal-Stärke stumm) und sicherer Mobile-Unlock (Start-Button + erster `pointerdown`). **3D-Visuals (`?r3d=1`):** 2D-Konfetti/Farb-Flash ersetzt durch dezente Kontakt-FX (Licht-Glint + perspektivisch projizierter Impuls-Ring bei Kollisionen, Staub-Puffs beim Ringout/Fall); Aim-Overlay „luxury minimal" — nur der eigene Aim sichtbar (keine Reveal-/Gegner-Pfeile), dünner Anthrazit-Strahl mit feiner Chevron-Spitze und hellem Saum, freistehende editorial Prozentzahl mit Doppel-Pass-Halo (kein Chip, kein Power-Ring). 2D ohne Flag pixel-identisch; keine Physik-/Firebase-/Protokoll-Änderung (Golden 13/13 unverändert grün) (2026-07-08)

### Fixes
- fix: Mobile-Portrait-Framing-Bug im 3D-Modus (`?r3d=1`) behoben (M4-T2b, akzeptiert) — `#cv3d` hatte keine CSS-Größe; `renderer.setSize(…, false)` setzt nur die Backing-Auflösung (Viewport × DPR), wodurch das Canvas auf Geräten mit DPR > 1 (Handys) in intrinsischer Größe größer als der Bildschirm renderte → Arena nur als Ausschnitt sichtbar, Zielen verschoben. Fix: `width:100%;height:100%` in der `#cv3d`-CSS-Regel; Desktop (DPR 1) pixelidentisch. Kamera-Mathematik war korrekt (Portrait-Fit für 360×800/390×844/412×915 numerisch verifiziert). Mapping-Suite um statischen Regressions-Check erweitert (31 Fälle). Sichtprüfung Handy bestanden: Arena vollständig sichtbar, 60 FPS (Min 60), GLB ~1 555 ms, HDRI ~118 ms (2026-07-08)

### Tooling
- chore: Performance-Monitor `?perf=1` im Hauptspiel (M4-T2b) — FPS (500-ms-Mittel), Min-FPS über 10 s, bei `?r3d=1` zusätzlich GLB-/HDRI-Ladezeit; ohne Flag komplett inert (kein DOM-Element, kein Overhead). Portiert vom Prototyp-Monitor (2026-07-08)

### Features
- feat: 3D-Render-Adapter fürs Hauptspiel hinter `?r3d=1` (M4-T2, akzeptiert) — three.js-Layer im Prototyp-Look (GLB-Arena, HDRI-Himmel, Wolkenmeer, ferne Insel, Vignette) als Vollbild hinter der UI; **2D bleibt Standard/Fallback** (ohne Flag unverändert, jeder Lade-/WebGL-Fehler fällt sauber auf 2D zurück). Feste geneigte Basiskamera mit **bewegbarer Spieler-Kamera** (Drag außerhalb der Aim-Zone = drehen, Pinch/Mausrad = Zoom, Doppeltipp = Reset, Damping; Aim-Zone hat Vorrang, während Zielen keine Kamera und umgekehrt); `?r3d=1&orbit=1` = reiner Showcase-Modus mit deaktiviertem Zielen. Input via Raycasting/`s2w` (Ray-Ebene-Schnitt), Overlays punktweise per `w2s` projiziert — beides über pure, Node-getestete Mathematik (`tools/test_r3d_mapping.js`, 30 Fälle: Round-Trip <1e-6, P2-Spiegelung, Shift/Bob, freie Kamera; `localPt`-2D byte-identisch). **Ringout-Kante = sichtbare Außenkante** (Skalierung `R/10.1` auf die GLB-Randweg-Kante, Leuchtring/Warnzone/Goldrahmen dort); Kristalle/Sockel im Spiel-Renderer entfernt (Gameplay-Lesbarkeit, Asset unverändert); Kugeln als polierte Murmeln ohne Labels (Erkennung über Farbe/HUD); kosmetische Fall-Animation: Kugel driftet mit letztem Schwung über die Kante, fällt mit Gravity, rollt und verschwindet in den Wolken (rein lesend). Renderer schreibt nie Spielzustand; keine Physik-/Größen-/Protokoll-/Firebase-Änderung (2026-07-07)

### Tests
- test: Golden-Physik-Regressionssuite (M4-T1) — `tools/test_physics_golden.js` extrahiert die echten Simulationsfunktionen (`stepSim`, `simExchange`, `simSnap` + Konstanten) aus `index.html` und vergleicht 13 deterministische Referenzfälle bit-exakt gegen `tools/golden_physics.json` (inkl. Frame-30-Checkpoints, Ring-Out-Toleranz, Decisive-Ball-Logik, Spin/Magnus, Langsamfahrt-Regime, 2v2-Pile-ups, geschrumpfte Arena). Empfindlichkeit bewiesen: FRICTION+1e-7 lässt 13/13 Fälle fehlschlagen. Sicherheitsnetz vor der 3D-Render-Integration; `--update` nur für beabsichtigte Physikänderungen (dann mit Protocol-Bump) (2026-07-07)

### Tooling
- chore: Performance-Monitor für den 3D-Arena-Prototyp — FPS (500-ms-Mittel), Min-FPS über 10 s, GLB-/HDRI-Ladezeit; nur über URL-Parameter `?perf=1` sichtbar, ohne Flag komplett inert; keine Design-/Asset-Änderung. **Mobile-Performance-Test bestanden:** Handy 60 FPS (Min 60), GLB ~1 964 ms, HDRI ~957 ms; PC >150 FPS (2026-07-07)

### Visual
- feat: akzeptierter 3D-Arena-Visual-Prototyp (Stand final) — `prototype3d.html` mit Blender-/GLB-Asset-Pipeline: `assets/arena_platform.glb` (36 MB, vollständig eigenständig, 9 eingebettete PBR-Maps, Clearcoat/Transmission/Texture-Transform-Extensions) wird per Skript `tools/build_arena_platform.py` headless aus Blender 4.4 generiert; CC0-Assets (ambientCG Marble012/Rock030, Poly Haven stone_brick_wall_001 + 2 Puresky-HDRIs). Look: helle Marmor-Tempelplattform im Golden-Hour-Himmel, versenkte Gold-Inlays, Kristall-Sockel mit Marmor-Fassung, gestufter Tempel-Unterbau, dreiteilige Gameplay-Grenze (Randzone + Leuchtring + Goldrahmen). Gameplay-Sizing für 4–5 Spieler: Spielfläche +44 % (Radius ×1,2 via `PLAY_SCALE`), Kugeln −6,5 %; Kamera-Tilt auf faire ~62° geklemmt; alle Partikel-/Staub-Effekte entfernt. Roh-Texturen (66 MB) und `.blend`-Master bewusst nicht im Repo (`.gitignore`), re-generierbar per Skript + dokumentierten CC0-Downloads. Keine Integration ins Hauptspiel (2026-07-07)
- feat: isolierter Three.js-Visual-Spike `prototype3d.html` — echter WebGL-3D-Prototyp der Arena (schwebende Marmor-/Stein-Plattform mit zerklüftetem Fels-Unterbau, Gold-Inlays, 8 Kristall-Sockel, zwei Hochglanz-Kugeln, Wolkenmeer + ferne Wolkeninseln, warme Sonnenstimmung, Orbit-Kamera mit geklemmtem Tilt). Drei Polish-Pässe durchlaufen. **Status: technisch erfolgreich, visuell noch nicht final** — für den Premium-Look der Referenzbilder fehlen echte Assets (Marmor-/Stein-Texturen, volumetrischere Wolken). Dependency: three@0.170.0 nur via CDN-Importmap in dieser einen Datei. **Keine Integration ins Hauptspiel** — `index.html`, Spiel-Logik, Physik, Online, Replay und Firebase unberührt (2026-07-06)
- feat: 3D-Arena-Erlebnis + Kamera-Steuerung (M2-T2) — Pseudo-3D/Hybrid auf Canvas 2D: orthografische Kamera (Yaw frei 360°, Pitch geklemmt 0–35°, Default ~20°), Drag auf leerer Fläche dreht die Ansicht (Drag auf eigener Kugel zielt wie bisher, 2. Finger bleibt Drall — jetzt in Screen-Pixeln). Heller Wolkenhimmel mit Sonne und Parallaxe (einmalig gebacken), Marmorboden mit Adern/Gold-Ringlinien/Kompassrose (einmalig gebacken → pro Frame nur drawImage, zugleich Perf-Cache), zweistufige Zylinder-Wand mit Steinbändern (sichtbare Plattform-Dicke), Schlagschatten auf Wolkenebene, 8 Kristall-Sockel am Rand, exakte Rauswurf-Grenze als projizierte Ellipse. Kugeln als Billboards (immer rund, tiefensortiert) mit glossy Spitzlicht, Nebenreflex, Boden-Gegenlicht. Input via exakter Kamera-Inverse — der alte 180°-Spiegel für Online-P2 ist jetzt Kamera-Default Yaw=180°. Kamera ist rein lokal (nie synchronisiert): Lockstep, Replay-Daten und Determinismus unberührt; keine Physik-, Protokoll- oder Firebase-Änderung (2026-07-06)
- feat: arena visual polish (M2-T1a) — kühles eisig-metallisches Material statt Gold-Look: Silber-/Stahlblau-Bodengradient mit metallischem Streiflicht und frostigem Innenleuchten, Eisglow statt Goldglow am Rand, pseudo-3D-Randwand (gestuftes Band für Tiefe), Schlagschatten-Ellipse unter der Plattform (Schwebe-Eindruck), kühl-dunkler Stahlblau-Hintergrund (kein heller Himmel), weicherer/klar definierter Kugel-Bodenschatten. Boden bewusst silbrig-neutral gehalten, damit blaue und rote Kugel klar sichtbar bleiben. Rein visuell — keine Physik, keine Konstanten, kein Determinismus-/Lockstep-/Replay-Einfluss (2026-07-06)

### Features
- feat: add Firebase Realtime Database security rules (`firebase.rules.json`) — enforce room schema server-side as a second line behind client validation: room code charset, immutable `v`/`config`/`created` after creation, write-once moves with value bounds mirroring `sanitizeMove` (idx exactly 0–3, dx/dy ±195, sp ±1), move writes only for the current `gen` (verified via `$gen === gen + ''` coercion), monotonic `gen` (+1 or idempotent equal), clean initial room (gen 0, host present, no guest/moves prefilled) guarded by `data.exists()` so it applies only at creation and never blocks later child writes, host presence delete-only (p/0 cannot be re-set after the host leaves), presence with 2h join window, no room enumeration (root read denied). `created` switched to `serverTimestamp()` so the rule `created === now` makes it server-authoritative. Published manually in the Firebase console and verified against the live DB (29/29 REST checks incl. gen-match in both directions) plus a manual two-tab match (create, join, moves, rematch, disconnect). App Check deferred (see TODO). Honest limit: without auth, anyone who knows a room code can still grief (take the guest slot, write valid moves, trigger gen +1) — rules only block invalid structure/values, overwrites, room-overwrite, and enumeration (2026-07-06)
- feat: add online protocol version check — `ONLINE_PROTOCOL_VERSION` (Integer, Start 1) wird von `createRoom()` atomar in den Raum geschrieben; `validateRoom()` lehnt fehlende oder abweichende Versionen strikt ab („Versionen stimmen nicht überein — bitte beide Seite neu laden."). Bump-Regel: nur bei Änderungen an Online-Protokoll, Raum-Schema, Lockstep, Physik oder Move-Daten — reine UI-/Grafik-/Textänderungen bumpen nicht (2026-07-06)

### Fixes
- fix: validate room data before joining online rooms — new pure `validateRoom()` rejects invalid/manipulated rooms instead of silently defaulting: `winTarget` strictly 3|5, `fmt` strictly single|double, `gen` safe integer 0–10 000, presence map checked (host must be present, room not full; Firebase array form supported). Validation runs before any state mutation in `joinRoom()` (2026-07-06)
- fix: clamp online move data (dx, dy, sp) before applying — prevents velocity injection by cheating clients. Deterministic, idempotent `sanitizeMove()` applied at both lockstep ends (sender in `commit()`, receiver in `onlineTurnValue()`): move vector magnitude clamped to `maxPull()`, spin to [−1, +1], ball index validated against ownership with fallback (2026-07-05)

### Dokumentation
- `CLAUDE.md`: Zusammenfassungs-Regel ergänzt — Antworten über ~15–20 Zeilen beginnen mit einer 2–4-zeiligen „Kurz gesagt:"-Zusammenfassung (2026-07-06)
- `CLAUDE.md`: Kommunikationsregel ergänzt — kompakte, entscheidungsorientierte Owner-Kommunikation: kurze Statuszeilen während der Arbeit, festes 6-Punkte-Briefing (Ziel/Warum jetzt/Änderung/Risiken/Tests/Empfehlung) und 6-Punkte-Abschlusszusammenfassung; technische Details nur auf ausdrückliche Anfrage (2026-07-06)
- `CLAUDE.md`: Sprachregel ergänzt — alle Erklärungen an den Projekt-Owner (Briefings, Abschluss-Zusammenfassungen, Statusmeldungen) immer auf Deutsch; Code, Commits, technische Doku und Dateiinhalte bleiben Englisch (2026-07-05)
- `CLAUDE.md`: Planning Workflow Schritt 5 ersetzt — vor jedem Task (ohne Größen-Ausnahme) ein Briefing aus 5–10 Sätzen (warum jetzt, was kann brechen, wie sieht Erfolg aus, kommerzieller 100 000-Spieler-Test), danach explizite Freigabe abwarten; Freigabe gilt nur pro Task (2026-07-05)
- `CLAUDE.md`: Feature Completion Workflow auf 11 Schritte erweitert — automatische Ausführung nach jedem Task, Push-Verifikation (Branch synchron mit origin), Abschlusskriterium „nothing to commit, working tree clean" und exakter Abschlusssatz (2026-07-05)
- `CLAUDE.md` um Abschnitt „AI-Assisted Development Rules" erweitert (2026-06-30)
- `README.md` als Schnellübersicht erstellt (2026-06-30)

---

## [0.1.0] — 2026-06-30

### Initiale Implementierung

**Kern-Spielmechanik**
- Physik-Engine: Steinschleuder-Eingabe, Velocitäts-Integration, 2 Sub-Steps pro Frame
- Elastische Kollisionen zwischen Kugeln (`REST = 0.25`)
- Reibungssystem mit Langsamfahrt-Koeffizient (`FRICTION = 0.992`, `FEND = 0.992`)
- Drall / Magnus-Effekt via zweitem Touch-Finger (`SPIN_K = 0.004`, Decay `SPIN_DECAY = 0.985`)
- Ring-Out-Erkennung: Kugel überschreitet Arena-Radius → Runde endet
- Arenaschrumpfung: −3 % Radius pro Runde, Minimum 80 % des Ausgangswerts
- Phase-Zustandsautomat: `aim → reveal (600 ms) → sim → result (950 ms) → aim`

**Spielmodi**
- vs Bot (Leicht / Mittel / Schwer)
- 2 Spieler lokal (Pass-and-Play mit Sichtschutz-Bildschirm)
- Online 1v1 via Firebase Realtime Database (Lockstep, Raumcode)

**Formate**
- Einzel 1v1: 1 Kugel je Spieler
- Doppel 2v2: 2 Kugeln je Spieler, verdeckte Auswahl pro Runde

**Bot-KI**
- Leicht: Zufallsrichtung mit ±60°-Rauschen, reduzierte Kraft
- Mittel: Heuristik – Angriff auf Gegner oder Rückzug zur Mitte
- Schwer 1v1: Minimax via `simExchange` (650 Schritte), Bewertung durch `evalExchange`
- Schwer 2v2: Minimax via `simSnap` (420 Schritte) + `bestRespN`

**Online-Multiplayer**
- Firebase Realtime Database, Raumcode (4 Zeichen, ohne verwechselbare Zeichen)
- Lockstep: deterministischer Spielfeldkoordinatensatz (`LOGICAL = 1000`)
- Disconnect-Handling (`onDisconnect().remove()`)
- Rematch via Generationszähler (`gen`)
- Spielfeld-Spiegelung für Spieler 2 (Canvas-Rotation um 180°)

**Rendering**
- Goldene Vektor-Arena: radiale Gradienten, konzentrische Ringe, Kompassrose, Innenmedaillon
- Kristallrand: dreischichtiger animierter Goldglow + Kardinal-Kristallmarker
- 4 animierte Fackelhalter außerhalb der Arena (Flammen-Halo, Bodenlicht)
- Optionale Bildintegration (`arena.jpg`) vorbereitet
- Kugeln: 3D-Sphären-Gradient, Rim-Light, Bewegungsspur, Randwarnung bei < 26 % Abstand
- Slingshot-Vorschau: Drall-Trajektorie, gestrichelte Kraftlinie, Kraftanzeige in %
- Enthüllungspfeile nach dem Commit
- Hintergrund-Flash (`bgPulse`) bei Treffer

**Partikel-System**
- Spawn bei Abschuss, Treffer und Ring-Out
- Eigenschaften: Position, Velocity, Lebensdauer, Größe, Farbe

**Replay-System**
- Frame-für-Frame-Aufzeichnung aller Physikzustände (max. 6 000 Frames)
- Wiedergabe: Play/Pause, Vor/Zurück (30-Frame-Sprünge), Geschwindigkeit 1× / ½× / ¼×
- Seek-Balken

**Audio (Web Audio API, prozedural)**
- Ladetonkontinuierlich moduliert mit Zugstärke (kein Stuck-Sound durch saubere Stop-Logik)
- Abschussklang, Trefferlaut, Ring-Out-Klang, Rundensieg-Fanfare, Match-Gewinn-Fanfare
- Lautstärke-Toggle (🔊 / 🔇)

**UI / UX**
- Hauptmenü: Modus-, Schwierigkeits-, Format- und Rundenzahl-Auswahl mit Pill-Buttons
- HUD: Spielerkarten, Punktestand, Rundenanzeige, Phasenstatus
- Toast-Benachrichtigungen (2,2 s)
- Game-Over-Overlay mit Rematch-, Replay- und Menü-Option
- Sichtschutz-Bildschirm für lokales 2-Spieler-Spiel
- Online-Lobby mit Raum erstellen / beitreten

**Projektdokumentation**
- `CLAUDE.md`: Coding Standards, Architektur-Richtlinien, Git-Workflow, Performance-Checkliste
- `PROJECT.md`: Aktueller Projektstand (dieses Release)
- `ROADMAP.md`: Langfristige Ziele in 6 Phasen
- `TODO.md`: Offene Aufgaben nach Priorität (P0–P3)
- `CHANGELOG.md`: Dieses Dokument

---

_Format basiert auf [Keep a Changelog](https://keepachangelog.com/de/1.0.0/)._
